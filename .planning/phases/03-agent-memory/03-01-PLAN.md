---
phase: 03-agent-memory
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - server/memory/__init__.py
  - server/memory/agent_memory.py
  - server/tests/test_agent_memory.py
autonomous: true
requirements:
  - AMEM-01
  - AMEM-02
  - AMEM-03

must_haves:
  truths:
    - "AgentMemory.record()를 호출하면 data/memory/{agent}_memory.json 파일이 생성되고 레코드가 추가된다"
    - "AgentMemory.load_relevant()를 호출하면 태스크 유형이 일치하는 최신 N건의 경험이 반환된다"
    - "레코드가 MAX_DETAIL_COUNT를 초과하면 오래된 항목이 압축(요약)되고 파일 크기가 제한된다"
    - "load_relevant()는 파일 로드 시 lazy compaction을 자동 수행한다"
  artifacts:
    - path: "server/memory/agent_memory.py"
      provides: "AgentMemory 클래스 — record/load_relevant/compact 메서드"
      exports: ["AgentMemory", "MemoryRecord"]
    - path: "server/tests/test_agent_memory.py"
      provides: "AgentMemory 단위 테스트"
      contains: "test_record_creates_file, test_load_relevant_filters, test_lazy_compaction"
  key_links:
    - from: "server/memory/agent_memory.py"
      to: "data/memory/{agent}_memory.json"
      via: "atomic write (tmp+rename)"
      pattern: "os\\.rename|tmp"
    - from: "server/memory/agent_memory.py"
      to: "MemoryRecord"
      via: "json.dumps / json.loads"
      pattern: "task_id.*success.*feedback.*tags.*timestamp"
---

<objective>
에이전트 메모리 핵심 모듈(AgentMemory)을 설계하고 구현한다.

Purpose: OrchestrationLoop가 QA/Claude 피드백을 기록하고 작업 시작 시 경험을 주입하려면 단일 진입점 클래스가 필요하다.
Output: server/memory/agent_memory.py (AgentMemory + MemoryRecord), 단위 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-agent-memory/03-CONTEXT.md

<interfaces>
<!-- Phase 1/2 구현체 — 재사용 패턴 참조용 -->

server/workspace/manager.py (atomic write 패턴):
```python
def write_artifact(self, relative_path: str, content: str | bytes) -> Path:
    target = self.safe_path(relative_path)
    target.parent.mkdir(parents=True, exist_ok=True)
    tmp_path = target.with_suffix(target.suffix + f'.tmp.{os.getpid()}')
    try:
        with open(tmp_path, 'w', encoding='utf-8') as f:
            f.write(content)
        os.rename(tmp_path, target)  # macOS APFS에서 원자적
    except Exception:
        tmp_path.unlink(missing_ok=True)
        raise
    return target
```

server/runners/json_parser.py:
```python
def parse_json(text: str) -> Any | None: ...  # 2-pass: strict → repair
```

server/tests/conftest.py (테스트 패턴):
```python
@pytest.fixture
def tmp_workspace(tmp_path):
    ws = tmp_path / 'workspace'
    ws.mkdir()
    return ws
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MemoryRecord 스키마 + AgentMemory 클래스 구현</name>
  <files>server/memory/__init__.py, server/memory/agent_memory.py</files>
  <behavior>
    - record() 호출 → data/memory/planner_memory.json 파일 생성
    - record() 두 번 호출 → 파일에 레코드 2개 누적
    - load_relevant(agent='planner', task_type='design') → task_type 일치 항목만 반환
    - load_relevant() → 반환 수 MAX_DETAIL_COUNT 이하
    - record() 호출이 MAX_DETAIL_COUNT 초과 시 load_relevant() 내 lazy compaction 실행 → 오래된 항목이 summary 필드로 교체
    - memory_root 파라미터로 테스트 격리 가능 (tmp_path 주입)
  </behavior>
  <read_first>
    - server/workspace/manager.py (atomic write 패턴 — os.rename 사용)
    - server/runners/json_parser.py (parse_json 재사용 여부 확인)
    - .planning/phases/03-agent-memory/03-CONTEXT.md (D-01~D-08 결정사항)
  </read_first>
  <action>
    server/memory/__init__.py: 빈 파일 생성 (네임스페이스 패키지).

    server/memory/agent_memory.py 구현:

    1. MemoryRecord (dataclass 또는 TypedDict):
       - task_id: str
       - task_type: str  # 'planning' | 'design' | 'development' | 'qa' | 기타
       - success: bool
       - feedback: str   # QA 불합격 사유 또는 Claude 보완 지시
       - tags: list[str] # 패턴 태그 (예: ['json_parse_error', 'missing_field'])
       - timestamp: str  # ISO 8601

    2. AgentMemory 클래스:
       - __init__(self, agent_name: str, memory_root: str | Path = 'data/memory')
         - self._file: Path = Path(memory_root) / f'{agent_name}_memory.json'
         - self._file.parent.mkdir(parents=True, exist_ok=True)
         - MAX_DETAIL_COUNT = 20  # 상세 경험 최대 보관 수 (D-07 재량)

       - record(self, record: MemoryRecord) -> None
         - 현재 파일 로드 (없으면 빈 dict 사용)
         - data['records'] 리스트에 record 추가 (MemoryRecord를 dict로 직렬화)
         - atomic write: tmp + os.rename (per workspace/manager.py 패턴)

       - load_relevant(self, task_type: str | None = None, limit: int = 5) -> list[MemoryRecord]
         - 파일 없으면 [] 반환
         - 파일 로드 후 즉시 _maybe_compact() 실행 (D-08 lazy compaction)
         - task_type이 None이 아니면 task_type 일치 항목만 필터
         - 최신 limit건 반환 (timestamp 기준 내림차순, 최신이 먼저)
         - compaction 후 파일이 변경됐으면 atomic write로 저장

       - _maybe_compact(self, data: dict) -> bool
         - records 수가 MAX_DETAIL_COUNT 이하면 False 반환 (압축 불필요)
         - 최신 MAX_DETAIL_COUNT // 2건은 상세 유지
         - 나머지(오래된) 건들은 규칙 기반 요약으로 교체:
           summary = f"[압축] {len(old_records)}건: 성공={n_success}, 실패={n_fail}, 주요태그={top_tags}"
           data['summary'] 키에 요약 문자열을 누적 (기존 summary가 있으면 append)
           data['records']에서 오래된 건 제거
         - True 반환 (압축 발생)

    금지사항:
    - jsonwebtoken, 외부 LLM 호출 금지 (D-07 재량: 규칙 기반 요약)
    - WorkspaceManager 재사용하지 말 것 — 경로 안전성 로직이 task_id 기반이라 부적합
    - !important, 인라인 스타일 없음 (Python 파일이므로 해당 없음)
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_agent_memory.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - server/memory/agent_memory.py 존재, AgentMemory와 MemoryRecord export
    - record() 호출 후 data/memory/{agent}_memory.json 파일에 레코드가 저장됨
    - load_relevant(task_type='x') 가 task_type 일치 항목만 반환
    - MAX_DETAIL_COUNT 초과 시 lazy compaction이 실행되어 records 수가 줄어듦
    - pytest tests/test_agent_memory.py 전부 PASS
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AgentMemory 단위 테스트 작성 (RED 먼저, GREEN으로 종료)</name>
  <files>server/tests/test_agent_memory.py</files>
  <behavior>
    - test_record_creates_file: tmp_path 기반 memory_root로 AgentMemory 생성 후 record() → 파일 존재 확인
    - test_record_accumulates: record() 두 번 호출 → load_relevant() 반환 수 2
    - test_load_relevant_filters_by_task_type: task_type='design' 레코드와 'development' 레코드 각 1건 저장 → load_relevant(task_type='design') 반환 1건
    - test_load_relevant_empty_when_no_file: 파일 없는 경로 → load_relevant() == []
    - test_lazy_compaction_triggers: MAX_DETAIL_COUNT + 1건 삽입 후 load_relevant() 호출 → records 수 < MAX_DETAIL_COUNT + 1 (압축 확인)
    - test_atomic_write_no_partial_file: record() 중 예외 발생 시 기존 파일 손상 없음 (tmp 파일 cleanup 확인)
  </behavior>
  <read_first>
    - server/tests/conftest.py (tmp_workspace fixture 패턴)
    - server/memory/agent_memory.py (Task 1 구현 후 읽기)
  </read_first>
  <action>
    테스트 파일 작성 원칙:
    - conftest.py의 tmp_path fixture 활용 — memory_root=tmp_path / 'memory' 주입으로 격리
    - 각 테스트는 독립적 tmp_path 사용 (pytest 자동 분리)
    - 절대 임포트: from memory.agent_memory import AgentMemory, MemoryRecord
    - 타임스탬프는 datetime.utcnow().isoformat()로 생성
    - test_atomic_write_no_partial_file: monkeypatch로 os.rename을 예외 발생으로 교체 후 실행,
      tmp 파일이 남아 있지 않음 + 기존 파일 내용 불변 검증

    RED 확인: Task 1 구현 전에 테스트 파일을 먼저 작성하여 import 에러 또는 AssertionError로 실패 확인.
    GREEN 확인: Task 1 구현 완료 후 pytest 전부 통과 확인.
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_agent_memory.py -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - server/tests/test_agent_memory.py 존재, 6개 이상 테스트 함수
    - pytest tests/test_agent_memory.py 전부 PASS (0 failures)
    - 각 테스트가 tmp_path로 격리되어 실제 data/memory/ 디렉토리를 오염하지 않음
  </done>
</task>

</tasks>

<verification>
cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_agent_memory.py -v
</verification>

<success_criteria>
- server/memory/__init__.py, server/memory/agent_memory.py 생성
- AgentMemory, MemoryRecord 클래스 export
- record() → atomic write로 data/memory/{agent}_memory.json 저장
- load_relevant() → task_type 필터 + lazy compaction
- 테스트 6개 이상 PASS, 실제 data/ 미오염
</success_criteria>

<output>
완료 후 .planning/phases/03-agent-memory/03-01-SUMMARY.md 생성
</output>
