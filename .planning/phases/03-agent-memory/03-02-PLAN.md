---
phase: 03-agent-memory
plan: '02'
type: execute
wave: 2
depends_on:
  - '03-01'
files_modified:
  - server/orchestration/loop.py
  - server/tests/test_orchestration.py
autonomous: true
requirements:
  - AMEM-02
  - AMEM-03

must_haves:
  truths:
    - "_run_agent() 실행 전 해당 에이전트의 과거 경험이 시스템 프롬프트에 추가된다"
    - "QA 불합격(_run_qa_gate() 반환 False) 시 즉시 해당 에이전트의 경험 파일에 실패 레코드가 저장된다"
    - "Claude 최종 검증 실패(_claude_final_verify() 반환 False) 시 관련 에이전트들의 경험 파일에 보완 지시가 기록된다"
    - "경험 주입은 기존 시스템 프롬프트를 덮어쓰지 않고 뒤에 추가(append)된다"
  artifacts:
    - path: "server/orchestration/loop.py"
      provides: "메모리 통합 OrchestrationLoop"
      contains: "AgentMemory"
    - path: "server/tests/test_orchestration.py"
      provides: "메모리 통합 테스트"
      contains: "test_memory_record_on_qa_fail, test_memory_inject_on_run_agent"
  key_links:
    - from: "server/orchestration/loop.py (_run_agent)"
      to: "server/memory/agent_memory.py (load_relevant)"
      via: "system_prompt += experience_section"
      pattern: "load_relevant|experience"
    - from: "server/orchestration/loop.py (_run_qa_gate)"
      to: "server/memory/agent_memory.py (record)"
      via: "즉시 record() 호출 (D-05)"
      pattern: "AgentMemory.*record|memory.*record"
    - from: "server/orchestration/loop.py (_claude_final_verify)"
      to: "server/memory/agent_memory.py (record)"
      via: "FAIL 분기에서 즉시 record() 호출 (D-06)"
      pattern: "AgentMemory.*record|memory.*record"
---

<objective>
OrchestrationLoop에 AgentMemory를 통합하여 에이전트 경험 자동 기록과 프롬프트 주입을 완성한다.

Purpose: AMEM-02(경험 참조)와 AMEM-03(피드백 자동 반영)은 loop.py의 핵심 실행 흐름에서만 구현 가능하다.
Output: 메모리 통합 loop.py, 통합 테스트 Green
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-agent-memory/03-CONTEXT.md
@.planning/phases/03-agent-memory/03-01-SUMMARY.md

<interfaces>
<!-- loop.py 현재 구현 — 수정할 세 지점 -->

OrchestrationLoop.__init__ 시그니처 (현재):
```python
def __init__(self, bus, runner, event_bus, workspace, router):
    ...
    self._task_graph: TaskGraph | None = None
    # 추가 예정: self._memory_root: Path
```

_load_agent_prompt (현재):
```python
def _load_agent_prompt(self, agent_name: str) -> str:
    prompt_path = AGENTS_DIR / f'{agent_name}.md'
    if prompt_path.exists():
        return prompt_path.read_text(encoding='utf-8')
    return ''
```

_run_agent 핵심 부분 (현재):
```python
system_prompt = self._load_agent_prompt(node.assigned_to)
prompt = f'[작업 지시]\n{node.description}\n\n[원본 요구사항]\n{node.requirements}'
result_data = await self.runner.generate_json(prompt, system=system_prompt)
```

_run_qa_gate 핵심 부분 (현재):
```python
if result_data.get('status') == 'success':
    return True
else:
    node.failure_reason = result_data.get('failure_reason', 'QA 불합격')
    return False
```

_claude_final_verify FAIL 분기 (현재):
```python
self._revision_count += 1
if self._revision_count >= self.MAX_REVISION_ROUNDS:
    self._state = WorkflowState.ESCALATED
    ...
else:
    self._state = WorkflowState.REVISION_LOOPING
    ...
return False
```

AgentMemory (03-01에서 구현):
```python
class AgentMemory:
    def __init__(self, agent_name: str, memory_root: str | Path = 'data/memory')
    def record(self, record: MemoryRecord) -> None
    def load_relevant(self, task_type: str | None = None, limit: int = 5) -> list[MemoryRecord]

class MemoryRecord:  # dataclass
    task_id: str
    task_type: str
    success: bool
    feedback: str
    tags: list[str]
    timestamp: str
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _run_agent()에 경험 주입 (AMEM-02, D-03, D-04)</name>
  <files>server/orchestration/loop.py</files>
  <behavior>
    - _run_agent() 호출 시 assigned_to 에이전트의 load_relevant(task_type, limit=5) 결과가 system_prompt에 append된다
    - 경험이 없으면 system_prompt는 기존과 동일 (빈 추가 없음)
    - 경험이 있으면 "\n\n## 이전 경험\n{경험 요약}" 형태로 기존 프롬프트 뒤에 추가
    - OrchestrationLoop.__init__ 에 memory_root: str | Path = 'data/memory' 파라미터 추가
  </behavior>
  <read_first>
    - server/orchestration/loop.py (전체 파일 — 수정 전 반드시 읽기)
    - server/memory/agent_memory.py (03-01 결과 — AgentMemory 인터페이스 확인)
    - .planning/phases/03-agent-memory/03-CONTEXT.md (D-03, D-04)
  </read_first>
  <action>
    server/orchestration/loop.py 수정 사항:

    1. import 추가 (파일 상단, 기존 import 블록 아래):
       ```python
       from datetime import datetime, timezone
       from memory.agent_memory import AgentMemory, MemoryRecord
       ```

    2. OrchestrationLoop.__init__ 파라미터 추가:
       ```python
       def __init__(self, bus, runner, event_bus, workspace, router,
                    memory_root: str | Path = 'data/memory'):
           ...
           self._memory_root = Path(memory_root)
       ```

    3. _run_agent() 에서 system_prompt 생성 직후, runner.generate_json() 호출 직전:
       ```python
       system_prompt = self._load_agent_prompt(node.assigned_to)

       # 이전 경험 주입 (AMEM-02, D-03)
       memory = AgentMemory(node.assigned_to, memory_root=self._memory_root)
       task_type = node.assigned_to  # 에이전트 이름을 task_type으로 활용
       experiences = memory.load_relevant(task_type=task_type, limit=5)
       if experiences:
           lines = []
           for exp in experiences:
               status_str = '성공' if exp.success else '실패'
               lines.append(f'- [{status_str}] {exp.feedback} (태그: {", ".join(exp.tags)})')
           system_prompt += '\n\n## 이전 경험\n' + '\n'.join(lines)
       ```

    주의:
    - 기존 코드 구조 (if result_data and isinstance...) 유지
    - PathLike 임포트 불필요 — 이미 pathlib.Path 임포트됨
    - memory_root는 절대경로가 아니므로 테스트 시 tmp_path 주입으로 격리
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_orchestration.py -x -q -k 'memory_inject or run_agent' 2>&1 | tail -20</automated>
  </verify>
  <done>
    - loop.py에 memory import 추가, __init__에 memory_root 파라미터 존재
    - _run_agent() 내 AgentMemory.load_relevant() 호출 코드 존재
    - 경험 있을 때 system_prompt에 "## 이전 경험" 섹션이 추가됨
    - 기존 테스트(test_orchestration.py) 깨지지 않음
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _run_qa_gate() + _claude_final_verify()에 경험 기록 (AMEM-03, D-05, D-06)</name>
  <files>server/orchestration/loop.py, server/tests/test_orchestration.py</files>
  <behavior>
    - _run_qa_gate()가 False 반환 시 → AgentMemory(node.assigned_to).record(MemoryRecord(success=False, feedback=node.failure_reason, ...)) 즉시 호출
    - _claude_final_verify()가 False 반환 시 → 완료 태스크 각 에이전트에 record(success=False, feedback=claude_response) 호출
    - record()는 QA/Claude 결과 반환 전에 완료 (blocking, 즉시)
    - _run_agent()에서 result.status == 'success'일 때도 성공 record() 저장 (AMEM-01: 성공 패턴도 기록)
    - 테스트: MagicMock으로 AgentMemory를 patch하여 record() 호출 여부 검증
  </behavior>
  <read_first>
    - server/orchestration/loop.py (Task 1 수정 후 최신 버전)
    - server/tests/test_orchestration.py (기존 테스트 확인 — 패치 대상 네임스페이스 파악)
    - server/tests/conftest.py (fixture 패턴)
  </read_first>
  <action>
    server/orchestration/loop.py 수정 사항:

    1. _run_qa_gate() — QA 불합격 분기(node.failure_reason 설정 직후):
       ```python
       # QA 불합격 즉시 경험 기록 (AMEM-03, D-05)
       memory = AgentMemory(node.assigned_to, memory_root=self._memory_root)
       memory.record(MemoryRecord(
           task_id=node.task_id,
           task_type=node.assigned_to,
           success=False,
           feedback=node.failure_reason,
           tags=['qa_fail'],
           timestamp=datetime.now(timezone.utc).isoformat(),
       ))
       return False
       ```

    2. _run_agent() — result.status == 'success' 분기에도 성공 기록:
       ```python
       # 성공 경험 기록 (AMEM-01: 성공 패턴도 저장)
       success_memory = AgentMemory(node.assigned_to, memory_root=self._memory_root)
       success_memory.record(MemoryRecord(
           task_id=node.task_id,
           task_type=node.assigned_to,
           success=True,
           feedback=result.summary,
           tags=['success'],
           timestamp=datetime.now(timezone.utc).isoformat(),
       ))
       ```
       (result.status != 'success' FAILED 분기에는 기록 불필요 — QA gate에서 별도 기록)

    3. _claude_final_verify() — FAIL 분기(self._revision_count += 1 이전):
       ```python
       # Claude 보완 지시 즉시 경험 기록 (AMEM-03, D-06)
       for node in task_graph._nodes.values():
           if node.status == TaskStatus.DONE:
               mem = AgentMemory(node.assigned_to, memory_root=self._memory_root)
               mem.record(MemoryRecord(
                   task_id=node.task_id,
                   task_type=node.assigned_to,
                   success=False,
                   feedback=f'Claude 최종검증 불합격: {response[:200]}',
                   tags=['claude_revision'],
                   timestamp=datetime.now(timezone.utc).isoformat(),
               ))
       ```

    server/tests/test_orchestration.py 수정 사항:
    - 기존 테스트 중 OrchestrationLoop 생성 코드에 memory_root=tmp_path/'memory' 추가
    - 다음 테스트 함수 추가:

    test_memory_record_on_qa_fail:
      - mock AgentMemory.record (patch 'orchestration.loop.AgentMemory')
      - QA가 실패 응답 반환하도록 runner mock 설정
      - loop 실행 후 record() 호출 확인 (success=False, tags=['qa_fail'])

    test_memory_inject_on_run_agent:
      - AgentMemory.load_relevant mock → 경험 1건 반환
      - runner.generate_json mock → 경험이 포함된 system 인자 확인
      - '## 이전 경험' 문자열이 system prompt에 포함됨 검증

    patch 대상: 'orchestration.loop.AgentMemory' (직접 임포트된 네임스페이스)
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_orchestration.py -v 2>&1 | tail -40</automated>
  </verify>
  <done>
    - _run_qa_gate() False 반환 시 AgentMemory.record(success=False, tags=['qa_fail']) 호출
    - _run_agent() success 시 AgentMemory.record(success=True) 호출
    - _claude_final_verify() FAIL 시 완료 노드 각 에이전트에 record(tags=['claude_revision']) 호출
    - test_memory_record_on_qa_fail, test_memory_inject_on_run_agent PASS
    - 기존 test_orchestration.py 테스트 전부 PASS (깨짐 없음)
  </done>
</task>

</tasks>

<verification>
cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_orchestration.py tests/test_agent_memory.py -v 2>&1 | tail -50
</verification>

<success_criteria>
- loop.py에 AgentMemory import, memory_root 파라미터, 세 지점 통합 완료
- QA 불합격 → 즉시 실패 경험 기록
- Claude FAIL → 즉시 보완 지시 기록
- 작업 성공 → 성공 경험 기록
- 작업 시작 → 과거 경험 system_prompt 주입
- 기존 테스트 전부 PASS, 신규 테스트 PASS
</success_criteria>

<output>
완료 후 .planning/phases/03-agent-memory/03-02-SUMMARY.md 생성
</output>
