---
phase: 02-orchestration-workflow
plan: 05
type: execute
wave: 5
depends_on: [02-01, 02-02, 02-03, 02-04]
files_modified:
  - server/orchestration/loop.py
  - server/agents/designer.md
  - server/agents/developer.md
  - server/agents/qa.md
  - server/agents/planner.md
autonomous: true
gap_closure: true
requirements: [ORCH-02]

must_haves:
  truths:
    - 'loop.py가 런타임에 로드하는 에이전트 프롬프트 파일이 테스트가 검증하는 파일과 동일하다'
    - 'designer, developer 에이전트 프롬프트에 협업 규칙 섹션이 포함되어 있다'
    - '57개 전체 테스트가 수정 후에도 통과한다'
  artifacts:
    - path: 'server/orchestration/loop.py'
      provides: 'AGENTS_DIR이 루트 agents/ 또는 server/agents/의 완전한 파일을 가리킴'
      contains: '협업 규칙'
    - path: 'server/agents/designer.md'
      provides: '협업 규칙 섹션 포함 디자이너 프롬프트'
      contains: '협업 규칙'
    - path: 'server/agents/developer.md'
      provides: '협업 규칙 섹션 포함 개발자 프롬프트'
      contains: '협업 규칙'
  key_links:
    - from: 'server/orchestration/loop.py'
      to: 'agents/*.md (실제 로드 경로)'
      via: 'AGENTS_DIR / f"{agent}.md" 파일 읽기'
      pattern: 'AGENTS_DIR'
---

<objective>
런타임 경로(server/agents/)와 테스트 경로(root agents/)의 불일치를 해소하여 테스트 통과가 실제 런타임 품질을 보장하도록 한다.

Purpose: VERIFICATION.md가 발견한 유일한 구조적 갭 — designer.md, developer.md의 협업 규칙 누락으로 인해 런타임 에이전트가 메시지 버스 규칙 없이 실행되는 문제를 수정한다.
Output: server/agents/ 디렉토리의 모든 에이전트 파일이 4섹션을 완비하거나, loop.py가 완전한 루트 agents/를 직접 가리킨다.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- 갭 원인: loop.py line 19 -->
<!-- AGENTS_DIR = Path(__file__).parent.parent / 'agents' -->
<!-- 해석 결과: server/agents/ (불완전한 파일 존재) -->
<!-- test_agents.py line 5: AGENTS_DIR = Path(__file__).parent.parent.parent / 'agents' -->
<!-- 해석 결과: agents/ (루트, 4섹션 완비) -->

<interfaces>
<!-- loop.py AGENTS_DIR 현재 값 -->
<!-- server/orchestration/loop.py line 19: -->
AGENTS_DIR = Path(__file__).parent.parent / 'agents'
<!-- 이 경로는 server/agents/를 가리키며 designer.md, developer.md에 협업 규칙 없음 -->

<!-- test_agents.py AGENTS_DIR (변경 불필요) -->
<!-- server/tests/test_agents.py line 5: -->
AGENTS_DIR = Path(__file__).parent.parent.parent / 'agents'
<!-- 이 경로는 루트 agents/를 가리키며 4섹션 완비 -->

<!-- server/agents/designer.md 현재 내용 (섹션 3개만): -->
<!-- ## 역할 정의, ## JSON 출력 형식, ## 금지 사항 -->
<!-- 누락: ## 협업 규칙 -->

<!-- server/agents/developer.md 현재 내용 (섹션 3개만): -->
<!-- ## 역할 정의, ## JSON 출력 형식, ## 금지 사항 -->
<!-- 누락: ## 협업 규칙 -->

<!-- agents/designer.md 협업 규칙 섹션 (루트 — 완전한 버전): -->
## 협업 규칙
- 기획자로부터 `task_request` 메시지를 받아 작업을 시작합니다.
- 작업 완료 후 `task_result` 메시지로 결과를 반환합니다.
- 개발자에게 명세를 전달해야 할 때는 메시지 버스를 통해 `task_request` 타입으로 발행합니다.
- 모든 산출물은 workspace 내에 파일로 저장하고 `artifact_paths`에 경로를 명시합니다.
- 작업 진행 상황은 `status_update` 타입 메시지로 수시로 보고합니다.

<!-- agents/developer.md 협업 규칙 섹션 (루트 — 완전한 버전): -->
## 협업 규칙
- 기획자 또는 디자이너로부터 `task_request` 메시지를 받아 작업을 시작합니다.
- 작업 완료 후 `task_result` 메시지로 결과를 반환합니다.
- 디자이너에게 명세가 필요할 때는 메시지 버스를 통해 `task_request`를 발행합니다 (WKFL-03 자유 요청).
- 모든 코드 파일은 workspace 내에 파일로 저장하고 `artifact_paths`에 경로를 명시합니다.
- 작업 진행 상황은 `status_update` 타입 메시지로 수시로 보고합니다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: server/agents/ 파일에 협업 규칙 섹션 추가</name>
  <files>server/agents/designer.md, server/agents/developer.md, server/agents/qa.md</files>

  <read_first>
    - server/agents/designer.md — 현재 3섹션 구조 확인 (역할 정의, JSON 출력 형식, 금지 사항)
    - server/agents/developer.md — 현재 3섹션 구조 확인
    - server/agents/qa.md — 현재 섹션 구조 확인 (협업 규칙 이름이 다른지 확인)
    - agents/designer.md — 루트의 협업 규칙 섹션 내용 참조
    - agents/developer.md — 루트의 협업 규칙 섹션 내용 참조
    - agents/qa.md — 루트의 협업 규칙 섹션 내용 참조
  </read_first>

  <action>
    server/agents/ 디렉토리의 세 파일에 협업 규칙 섹션을 추가한다. 루트 agents/ 파일의 내용을 기준으로 하되 server/agents/의 간결한 스타일에 맞춰 작성한다.

    **server/agents/designer.md 수정:**
    기존 `## 금지 사항` 섹션 앞에 다음 섹션을 삽입한다:

    ```
    ## 협업 규칙
    - 기획자로부터 `task_request` 메시지를 받아 작업을 시작한다
    - 작업 완료 후 `task_result` 메시지로 결과를 기획자에게 반환한다
    - 개발자에게 명세를 전달할 때는 메시지 버스를 통해 `task_request` 타입으로 발행한다
    - 모든 산출물은 workspace 내에 파일로 저장하고 `artifact_paths`에 경로를 명시한다
    - 작업 진행 상황은 `status_update` 타입 메시지로 보고한다
    ```

    **server/agents/developer.md 수정:**
    기존 `## 금지 사항` 섹션 앞에 다음 섹션을 삽입한다:

    ```
    ## 협업 규칙
    - 기획자 또는 디자이너로부터 `task_request` 메시지를 받아 작업을 시작한다
    - 작업 완료 후 `task_result` 메시지로 결과를 기획자에게 반환한다
    - 디자이너에게 명세가 필요할 때는 메시지 버스를 통해 `task_request`를 발행한다 (WKFL-03)
    - 모든 코드 파일은 workspace 내에 파일로 저장하고 `artifact_paths`에 경로를 명시한다
    - 작업 진행 상황은 `status_update` 타입 메시지로 보고한다
    ```

    **server/agents/qa.md 수정:**
    현재 파일에 `## 협업 규칙` 헤더가 없다 (중요 원칙 섹션이 있지만 이름이 다름). `## 금지 사항` 섹션 앞에 다음 섹션을 삽입한다:

    ```
    ## 협업 규칙
    - 기획자로부터 `task_request` 메시지(assigned_to: qa)를 받아 검수를 시작한다
    - 검수 완료 후 `task_result` 메시지로 결과를 기획자에게 반환한다
    - 불합격 시 failure_reason에 구체적 사유를 명시한다
    - 검수 진행 상황은 `status_update` 타입 메시지로 보고한다
    ```

    각 파일을 수정할 때 기존 내용을 변경하지 말고 섹션을 삽입한다.
  </action>

  <verify>
    <automated>grep -c '협업 규칙' /Users/johyeonchang/ai-office/server/agents/designer.md && grep -c '협업 규칙' /Users/johyeonchang/ai-office/server/agents/developer.md && grep -c '협업 규칙' /Users/johyeonchang/ai-office/server/agents/qa.md</automated>
  </verify>

  <acceptance_criteria>
    - grep '## 협업 규칙' server/agents/designer.md → 매칭 1건 이상
    - grep '## 협업 규칙' server/agents/developer.md → 매칭 1건 이상
    - grep '협업 규칙' server/agents/qa.md → 매칭 1건 이상
    - grep 'task_request' server/agents/designer.md → 매칭 존재 (메시지 버스 규칙 포함 확인)
    - grep 'task_request' server/agents/developer.md → 매칭 존재
    - grep 'WKFL-03' server/agents/developer.md → 매칭 존재 (자유 요청 규칙 포함 확인)
  </acceptance_criteria>

  <done>server/agents/의 designer.md, developer.md, qa.md 모두 협업 규칙 섹션을 포함한다</done>
</task>

<task type="auto">
  <name>Task 2: loop.py AGENTS_DIR을 루트 agents/로 수정하여 테스트-런타임 경로 통합</name>
  <files>server/orchestration/loop.py</files>

  <read_first>
    - server/orchestration/loop.py lines 1-25 — AGENTS_DIR 선언 위치 확인 (line 19)
    - server/tests/test_agents.py lines 1-10 — 테스트의 AGENTS_DIR 경로 확인 (루트 agents/ 가리킴)
  </read_first>

  <action>
    loop.py의 AGENTS_DIR 경로를 루트 agents/ 디렉토리를 가리키도록 수정한다.

    현재 (line 19):
    ```python
    AGENTS_DIR = Path(__file__).parent.parent / 'agents'
    ```
    `Path(__file__)` = server/orchestration/loop.py
    `.parent` = server/orchestration/
    `.parent` = server/
    `/ 'agents'` = server/agents/  ← 불완전한 디렉토리

    수정 후:
    ```python
    # 에이전트 시스템 프롬프트 파일 디렉토리 (프로젝트 루트 agents/)
    AGENTS_DIR = Path(__file__).parent.parent.parent / 'agents'
    ```
    `.parent.parent.parent` = ai-office/ (프로젝트 루트)
    `/ 'agents'` = ai-office/agents/  ← 4섹션 완비 디렉토리

    이 변경으로 루트 agents/는 테스트가 이미 검증하는 경로와 동일해진다.
    server/agents/는 Task 1에서 보완되었으므로 두 접근 모두 유효하지만,
    단일 디렉토리를 사용하는 이 방법이 이중 관리 문제를 근본적으로 해소한다.

    주석을 한국어로 업데이트하여 경로 의도를 명확히 한다.
  </action>

  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_agents.py tests/test_orchestration.py -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - grep "parent.parent.parent / 'agents'" server/orchestration/loop.py → 매칭 존재
    - grep "parent.parent / 'agents'" server/orchestration/loop.py → 매칭 없음 (이전 경로 제거됨)
    - cd server && uv run pytest tests/test_agents.py -q → 4 passed
    - cd server && uv run pytest -q → 22 passed (Phase 2 테스트 전체)
    - cd server && uv run pytest --tb=short -q → 57 passed (전체 회귀 테스트)
  </acceptance_criteria>

  <done>loop.py AGENTS_DIR이 루트 agents/를 가리키며, 57개 전체 테스트가 통과한다</done>
</task>

</tasks>

<verification>
두 태스크 완료 후 전체 검증:

```bash
# 1. 협업 규칙 섹션 존재 확인
grep '## 협업 규칙' server/agents/designer.md server/agents/developer.md server/agents/qa.md

# 2. loop.py 경로 수정 확인
grep 'AGENTS_DIR' server/orchestration/loop.py

# 3. 전체 테스트 통과 확인
cd server && uv run pytest --tb=short -q
```

예상 결과:
- 3개 파일 모두 `## 협업 규칙` 포함
- loop.py: `parent.parent.parent / 'agents'`
- 57 passed, 0 failed
</verification>

<success_criteria>
- server/agents/designer.md에 `## 협업 규칙` 섹션 존재 (메시지 버스 + task_request 규칙 포함)
- server/agents/developer.md에 `## 협업 규칙` 섹션 존재 (메시지 버스 + WKFL-03 자유 요청 규칙 포함)
- server/agents/qa.md에 `## 협업 규칙` 섹션 존재
- loop.py AGENTS_DIR이 루트 agents/ (4섹션 완비)를 가리킴
- 57개 전체 테스트 통과 — 테스트 경로와 런타임 경로 일치로 회귀 없음
- ORCH-02 요건 완전 충족: 4개 에이전트 모두 독립 시스템 프롬프트 4섹션 완비 (런타임 포함)
</success_criteria>

<output>
완료 후 `.planning/phases/02-orchestration-workflow/02-05-SUMMARY.md` 생성
</output>
