---
phase: 02-orchestration-workflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/tests/test_orchestration.py
  - server/tests/test_agents.py
  - server/tests/test_message_routing.py
  - server/tests/test_revision_loop.py
  - server/tests/test_task_graph.py
  - server/tests/test_qa_gate.py
  - server/tests/test_free_request.py
  - server/tests/test_planner_tracking.py
  - server/bus/payloads.py
  - agents/planner.md
  - agents/designer.md
  - agents/developer.md
  - agents/qa.md
autonomous: true
requirements:
  - ORCH-02
  - ORCH-03

must_haves:
  truths:
    - 'payload 스키마(TaskRequestPayload, TaskResultPayload, StatusUpdatePayload)가 Pydantic 모델로 정의되고 import 가능하다'
    - '4개 에이전트(planner, designer, developer, qa) 시스템 프롬프트 파일이 존재하며, 각각 역할 정의·JSON 출력 스키마·협업 규칙·금지사항을 포함한다'
    - 'Wave 0 테스트 stub 파일 8개가 존재하고 xfail(strict=False)로 선언되어 pytest가 통과한다'
  artifacts:
    - path: 'server/bus/payloads.py'
      provides: 'TaskRequestPayload, TaskResultPayload, StatusUpdatePayload Pydantic 모델'
      exports: ['TaskRequestPayload', 'TaskResultPayload', 'StatusUpdatePayload']
    - path: 'agents/planner.md'
      provides: '기획자(PM) 시스템 프롬프트'
      contains: 'JSON 출력 스키마, PM 추적 역할, 협업 규칙'
    - path: 'agents/designer.md'
      provides: '디자이너 시스템 프롬프트'
      contains: 'JSON 출력 스키마, 협업 규칙, 금지사항'
    - path: 'agents/developer.md'
      provides: '개발자 시스템 프롬프트'
      contains: 'JSON 출력 스키마, 협업 규칙, 금지사항'
    - path: 'agents/qa.md'
      provides: 'QA 시스템 프롬프트'
      contains: '원본 요구사항 독립 참조 규칙, JSON 출력 스키마'
    - path: 'server/tests/test_orchestration.py'
      provides: 'ORCH-01 테스트 stub'
    - path: 'server/tests/test_agents.py'
      provides: 'ORCH-02 테스트 stub'
    - path: 'server/tests/test_message_routing.py'
      provides: 'ORCH-03 테스트 stub'
    - path: 'server/tests/test_revision_loop.py'
      provides: 'ORCH-04 테스트 stub'
    - path: 'server/tests/test_task_graph.py'
      provides: 'WKFL-01 테스트 stub'
    - path: 'server/tests/test_qa_gate.py'
      provides: 'WKFL-02 테스트 stub'
    - path: 'server/tests/test_free_request.py'
      provides: 'WKFL-03 테스트 stub'
    - path: 'server/tests/test_planner_tracking.py'
      provides: 'WKFL-04 테스트 stub'
  key_links:
    - from: 'server/bus/payloads.py'
      to: 'server/orchestration/router.py'
      via: 'TaskRequestPayload import'
      pattern: 'from bus.payloads import'
    - from: 'agents/qa.md'
      to: 'OllamaRunner system field'
      via: 'Ollama /api/generate system 파라미터'
      pattern: '원본 요구사항.*독립.*참조'
---

<objective>
Wave 0: 테스트 계약 + 메시지 payload 스키마 + 에이전트 시스템 프롬프트 파일 생성

Purpose: 인터페이스-퍼스트 원칙. 후속 플랜(02~04)이 구현할 계약을 먼저 정의한다.
payload 스키마 없이 구현하면 에이전트 간 타입 계약이 없어 ORCH-03 위반.
에이전트 시스템 프롬프트 없이 OllamaRunner 호출 불가.
테스트 stub 없이 후속 구현 작업에서 Nyquist 위반 발생.

Output:
- server/bus/payloads.py (TaskRequestPayload, TaskResultPayload, StatusUpdatePayload)
- agents/{planner,designer,developer,qa}.md (4개 시스템 프롬프트)
- server/tests/test_*.py x8 (xfail stub)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-orchestration-workflow/02-CONTEXT.md
@.planning/phases/02-orchestration-workflow/02-RESEARCH.md

<interfaces>
<!-- Phase 1 기존 계약 — 수정 없이 사용 -->

server/bus/schemas.py:
```python
MessageType = Literal['task_request', 'task_result', 'status_update']
AgentId = Literal['claude', 'planner', 'developer', 'designer', 'qa', 'orchestrator']
Priority = Literal['normal', 'high', 'urgent']
Status = Literal['pending', 'processing', 'done', 'failed']

class AgentMessage(BaseModel):
    id: str
    type: MessageType
    from_agent: AgentId = Field(alias='from')
    to_agent: AgentId | Literal['broadcast'] = Field(alias='to')
    payload: Any          # Phase 2에서 TaskRequestPayload 등으로 교체 예정
    reply_to: Optional[str] = None
    priority: Priority = 'normal'
    tags: list[str] = Field(default_factory=list)
    metadata: dict[str, Any] = Field(default_factory=dict)
    created_at: datetime
    ack_at: Optional[datetime] = None
    status: Status = 'pending'
    model_config = {'populate_by_name': True}
```

server/tests/conftest.py (기존 fixture):
```python
@pytest.fixture
def in_memory_db(): ...        # SQLite 인메모리
@pytest.fixture
def tmp_workspace(tmp_path): ...  # 임시 workspace 루트
```

기존 테스트 패턴 (test_message_bus.py 참고):
- @pytest.mark.xfail(strict=False, reason='stub — Wave N에서 구현')
- 절대 import: from bus.payloads import TaskRequestPayload
- 한국어 docstring
- 2 spaces 들여쓰기
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Payload 스키마 Pydantic 모델 정의 (ORCH-03)</name>
  <files>server/bus/payloads.py</files>
  <read_first>
    - server/bus/schemas.py (AgentMessage 구조, 기존 타입 확인)
    - server/bus/message_bus.py (payload 사용 방식 확인)
  </read_first>
  <action>
    `server/bus/payloads.py` 를 신규 생성한다.

    파일 상단에 한국어 주석으로 목적 기술: "# 에이전트 간 payload 스키마 (ORCH-03)"

    포함할 Pydantic 모델 세 가지:

    1. TaskRequestPayload
       - task_id: str (uuid4 기본값)
       - description: str  # 수행할 작업 설명
       - requirements: str  # 원본 요구사항 — QA 독립 참조용 (D-08)
       - depends_on: list[str] = []  # DAG 의존 task_id 목록
       - assigned_to: str  # 실행 에이전트 id

    2. TaskResultPayload
       - task_id: str
       - status: Literal['success', 'fail']
       - artifact_paths: list[str] = []  # workspace 내 상대 경로
       - summary: str  # 작업 요약 (기획자 추적용 — WKFL-04)
       - failure_reason: str | None = None  # QA 반려 시 구체적 이유 (D-09)

    3. StatusUpdatePayload
       - task_id: str
       - state: str  # WorkflowState 열거값 (loop.py에서 정의 예정)
       - agent_id: str
       - note: str = ''

    모든 모델: Pydantic BaseModel 상속, type hint 필수, 필드별 한국어 주석.
    import: from pydantic import BaseModel, Field; from typing import Literal, Optional; import uuid
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run python -c "from bus.payloads import TaskRequestPayload, TaskResultPayload, StatusUpdatePayload; p = TaskRequestPayload(description='test', requirements='req', assigned_to='developer'); print('OK:', p.task_id)"</automated>
  </verify>
  <acceptance_criteria>
    - `from bus.payloads import TaskRequestPayload, TaskResultPayload, StatusUpdatePayload` 가 에러 없이 import됨
    - TaskRequestPayload 인스턴스 생성 시 task_id가 UUID 문자열로 자동 생성됨 (grep: `default_factory=lambda: str(uuid.uuid4())`)
    - TaskResultPayload.failure_reason 필드가 Optional[str] (None 허용) (grep: `failure_reason.*str.*None`)
    - TaskRequestPayload.requirements 필드 존재 — QA 독립 참조 근거 (grep: `requirements.*str`)
  </acceptance_criteria>
  <done>
    server/bus/payloads.py 존재, 세 모델 모두 import 가능, python 실행 OK 출력.
  </done>
</task>

<task type="auto">
  <name>Task 2: 에이전트 시스템 프롬프트 파일 4개 작성 (ORCH-02, D-02, D-03)</name>
  <files>
    agents/planner.md,
    agents/designer.md,
    agents/developer.md,
    agents/qa.md
  </files>
  <read_first>
    - .planning/phases/02-orchestration-workflow/02-CONTEXT.md (D-02, D-03, D-04~D-09 결정사항)
    - .planning/phases/02-orchestration-workflow/02-RESEARCH.md (Pattern 4 QA 독립 참조 패턴)
    - .planning/REQUIREMENTS.md (ORCH-02, WKFL-01~04)
  </read_first>
  <action>
    프로젝트 루트에 `agents/` 디렉토리를 생성하고 4개 마크다운 파일을 작성한다.

    각 파일은 D-02에 따라 반드시 4개 섹션을 포함해야 한다:
    1. ## 역할 정의 — 이 에이전트가 무엇을 하는가
    2. ## JSON 출력 형식 — 응답 스키마 (TaskRequestPayload 또는 TaskResultPayload 기반)
    3. ## 협업 규칙 — 다른 에이전트에게 요청하는 방법 (메시지 버스 경유)
    4. ## 금지사항 — 역할 외 작업 명시

    **agents/planner.md (기획자 + PM 겸임 — WKFL-01, WKFL-04):**
    - 역할: 사용자 지시를 분석하여 태스크 그래프를 구성하고, PM으로서 모든 에이전트의 작업 상태를 추적한다
    - JSON 출력: TaskRequestPayload 구조로 응답 (task_id, description, requirements, depends_on, assigned_to)
    - PM 추적 책임 명시: 모든 에이전트 간 요청/결과를 기록, 전체 흐름 파악 필수
    - 금지사항: 직접 코드 작성, 디자인 결정, QA 검수 수행 금지

    **agents/designer.md (디자이너):**
    - 역할: 기획자의 지시를 받아 UI/UX 설계, 컴포넌트 명세, 디자인 토큰을 산출물로 생성한다
    - JSON 출력: TaskResultPayload 구조 (task_id, status, artifact_paths, summary)
    - 협업: 개발자에게 명세 전달 시 메시지 버스(task_request) 사용
    - 금지사항: 코드 구현, 직접 QA 통과 선언 금지

    **agents/developer.md (개발자):**
    - 역할: 기획자 또는 디자이너의 명세를 받아 실제 코드를 workspace에 파일로 생성한다
    - JSON 출력: TaskResultPayload 구조 (artifact_paths에 실제 파일 경로 포함 필수)
    - 협업: 디자이너에게 명세 요청 시 메시지 버스 사용 가능 (WKFL-03)
    - 금지사항: QA 없이 완료 처리, 파일 외부 저장 금지

    **agents/qa.md (QA — D-07, D-08, D-09):**
    - 역할: 각 작업 완료 시 원본 요구사항을 독립적으로 참조하여 검수한다
    - 핵심 규칙(확증편향 방지): 작업 결과물만 보지 말고 반드시 task_request.requirements를 기준으로 검수
    - 입력 구조 명시:
      ```
      [원본 요구사항]
      {requirements}

      [작업 결과물 경로]
      {artifact_paths}

      위 요구사항 대비 결과물을 검수하라.
      ```
    - JSON 출력: TaskResultPayload (status: 'success'|'fail', failure_reason에 구체적 문제점 기술)
    - 금지사항: 결과물만 보고 판단, 원본 요구사항 미참조 상태에서 통과 선언
  </action>
  <verify>
    <automated>ls /Users/johyeonchang/ai-office/agents/planner.md /Users/johyeonchang/ai-office/agents/designer.md /Users/johyeonchang/ai-office/agents/developer.md /Users/johyeonchang/ai-office/agents/qa.md && grep -l '역할 정의' /Users/johyeonchang/ai-office/agents/*.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - agents/ 디렉토리가 프로젝트 루트(`/Users/johyeonchang/ai-office/agents/`)에 존재함
    - 4개 파일 모두 존재: planner.md, designer.md, developer.md, qa.md
    - 각 파일에 '역할 정의', 'JSON 출력', '협업 규칙', '금지사항' 섹션이 모두 포함됨 (grep: `grep -c '##' agents/*.md` 각 파일 4 이상)
    - agents/qa.md에 '원본 요구사항' 및 '확증편향' 또는 '독립' 키워드 포함 (grep: `grep -l '원본 요구사항' agents/qa.md`)
    - agents/planner.md에 PM 추적 역할 명시 (grep: `grep -l 'PM\|추적' agents/planner.md`)
  </acceptance_criteria>
  <done>
    4개 파일 존재, 각각 4개 섹션 완비, QA 파일에 독립 참조 규칙 포함.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 테스트 stub 8개 생성 (Nyquist 원칙)</name>
  <files>
    server/tests/test_orchestration.py,
    server/tests/test_agents.py,
    server/tests/test_message_routing.py,
    server/tests/test_revision_loop.py,
    server/tests/test_task_graph.py,
    server/tests/test_qa_gate.py,
    server/tests/test_free_request.py,
    server/tests/test_planner_tracking.py
  </files>
  <read_first>
    - server/tests/conftest.py (기존 fixture 확인)
    - server/tests/test_message_bus.py (기존 테스트 패턴 확인)
    - .planning/phases/02-orchestration-workflow/02-VALIDATION.md (각 파일이 커버할 요구사항 확인)
  </read_first>
  <action>
    8개 테스트 파일을 생성한다. 각 파일은 xfail(strict=False) 패턴으로 stub 테스트를 선언한다.
    기존 테스트 패턴(test_message_bus.py)과 동일한 스타일 유지: 2 spaces 들여쓰기, 한국어 docstring, 절대 import.

    **공통 패턴:**
    ```python
    import pytest

    @pytest.mark.xfail(strict=False, reason='stub — Wave N에서 구현')
    def test_XXX():
      '''한국어 설명 (요구사항 ID)'''
      assert False, '미구현'
    ```

    **각 파일별 stub 내용:**

    test_orchestration.py (ORCH-01):
    - test_claude_analyzes_user_instruction(): Claude가 사용자 지시를 파싱하여 기획자에게 task_request를 전달한다
    - test_claude_routes_only_to_planner(): Claude는 작업자에게 직접 지시하지 않고 반드시 기획자를 경유한다 (D-04)

    test_agents.py (ORCH-02):
    - test_planner_has_system_prompt(): 기획자 시스템 프롬프트 파일이 존재하고 필수 섹션을 포함한다
    - test_all_agents_have_system_prompts(): 4개 에이전트 모두 시스템 프롬프트 파일이 존재한다

    test_message_routing.py (ORCH-03):
    - test_task_request_payload_schema(): TaskRequestPayload가 Pydantic 모델로 유효성 검사된다
    - test_task_result_payload_schema(): TaskResultPayload가 Pydantic 모델로 유효성 검사된다
    - test_status_update_payload_schema(): StatusUpdatePayload가 Pydantic 모델로 유효성 검사된다

    test_revision_loop.py (ORCH-04):
    - test_claude_final_verification_pass(): Claude 최종 검증이 합격 결과를 반환한다
    - test_claude_final_verification_fail_triggers_revision(): 불합격 시 기획자를 경유한 보완 지시가 발행된다
    - test_max_revision_rounds_escalates(): 최대 반복 횟수(3) 초과 시 에스컬레이션 상태로 전환된다

    test_task_graph.py (WKFL-01):
    - test_task_graph_creates_node(): TaskGraph에 노드를 추가할 수 있다
    - test_task_graph_tracks_status(): 태스크 상태(pending/processing/done/failed)를 추적한다
    - test_task_graph_dependency_order(): 의존성이 있는 태스크는 선행 태스크 완료 후 실행 가능 상태가 된다

    test_qa_gate.py (WKFL-02):
    - test_qa_receives_original_requirements(): QA 에이전트가 원본 요구사항을 독립적으로 참조한다 (D-08)
    - test_qa_fail_returns_failure_reason(): QA 불합격 시 failure_reason이 구체적으로 포함된다 (D-09)

    test_free_request.py (WKFL-03):
    - test_developer_can_request_designer(): 개발자가 디자이너에게 작업 요청을 보낼 수 있다
    - test_free_request_goes_through_message_bus(): 자유 요청은 메시지 버스를 경유한다

    test_planner_tracking.py (WKFL-04):
    - test_planner_receives_broadcast_copy(): 모든 에이전트 간 메시지의 broadcast 복사가 기획자에게 전달된다
    - test_planner_tracks_all_task_states(): 기획자가 전체 에이전트의 태스크 상태를 추적한다
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_orchestration.py tests/test_agents.py tests/test_message_routing.py tests/test_revision_loop.py tests/test_task_graph.py tests/test_qa_gate.py tests/test_free_request.py tests/test_planner_tracking.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - 8개 파일 모두 존재 (`ls server/tests/test_orchestration.py` 등 각각 확인)
    - `uv run pytest tests/test_*.py` 실행 시 전체 exit code 0 (xfail은 통과로 처리됨)
    - xfail 마킹이 있는 테스트가 XFAIL 또는 XPASS로 표시되고 FAIL이 없음
    - 각 파일 상단에 요구사항 ID 주석 포함 (grep: `grep -l 'ORCH-\|WKFL-' server/tests/test_orchestration.py`)
  </acceptance_criteria>
  <done>
    8개 stub 파일 존재, pytest 전체 통과, xfail 마킹 완료.
  </done>
</task>

</tasks>

<verification>
cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_orchestration.py tests/test_agents.py tests/test_message_routing.py tests/test_revision_loop.py tests/test_task_graph.py tests/test_qa_gate.py tests/test_free_request.py tests/test_planner_tracking.py -v --tb=short

기존 테스트 suite도 깨지지 않는지 확인:
cd /Users/johyeonchang/ai-office/server && uv run pytest -x -q
</verification>

<success_criteria>
- server/bus/payloads.py 존재, 세 Pydantic 모델 import 가능
- agents/ 디렉토리에 4개 .md 파일 존재, 각각 역할정의/JSON출력/협업규칙/금지사항 4섹션 완비
- 8개 테스트 stub 파일 존재, pytest 전체 통과 (xfail 허용)
- 기존 Phase 1 테스트 suite 유지 (regression 없음)
</success_criteria>

<output>
완료 후 `.planning/phases/02-orchestration-workflow/02-01-SUMMARY.md` 를 생성한다.
</output>
