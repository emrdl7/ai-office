---
phase: 02-orchestration-workflow
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - server/main.py
  - server/tests/test_agents.py
autonomous: false
requirements:
  - ORCH-01
  - ORCH-02
  - ORCH-03
  - ORCH-04
  - ORCH-05
  - WKFL-01
  - WKFL-02
  - WKFL-03
  - WKFL-04

must_haves:
  truths:
    - 'POST /api/tasks 엔드포인트가 사용자 지시(instruction)를 받아 OrchestrationLoop.run()을 백그라운드로 실행한다'
    - 'GET /api/tasks/{task_id} 엔드포인트가 태스크 현재 상태를 반환한다'
    - 'main.py가 OrchestrationLoop 싱글턴을 lifespan에서 생성하고 MessageBus, OllamaRunner, EventBus, WorkspaceManager를 주입한다'
    - 'curl POST /api/tasks 요청 시 {"task_id": "...", "status": "accepted"} 응답이 반환된다'
    - '에이전트 시스템 프롬프트 파일(agents/*.md) 존재 여부를 검증하는 테스트가 GREEN이다'
  artifacts:
    - path: 'server/main.py'
      provides: 'POST /api/tasks 엔드포인트 + OrchestrationLoop 통합'
      contains: 'POST /api/tasks'
  key_links:
    - from: 'server/main.py'
      to: 'server/orchestration/loop.py'
      via: 'OrchestrationLoop 싱글턴 + asyncio.create_task'
      pattern: 'OrchestrationLoop\|create_task'
    - from: 'server/main.py'
      to: 'server/bus/message_bus.py'
      via: 'MessageBus 인스턴스 lifespan 생성'
      pattern: 'MessageBus\('
---

<objective>
FastAPI 진입점 통합 + 에이전트 프롬프트 파일 존재 테스트 GREEN

Purpose: Plan 01~03에서 구현한 컴포넌트를 main.py에 연결하여 실제 HTTP 요청을 통한 오케스트레이션 실행을 가능하게 한다.
이 플랜 완료 후 `curl -X POST /api/tasks -d '{"instruction":"..."}'` 로 전체 워크플로우가 실행된다.

Output:
- server/main.py (POST /api/tasks, GET /api/tasks/{task_id} 추가)
- server/tests/test_agents.py (stub → GREEN 전환)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-orchestration-workflow/02-CONTEXT.md
@.planning/phases/02-orchestration-workflow/02-03-SUMMARY.md

<interfaces>
<!-- 전체 Phase 2 산출물 계약 -->

server/orchestration/loop.py (Plan 03):
```python
class WorkflowState(str, Enum):
    IDLE = 'idle'
    CLAUDE_ANALYZING = 'claude_analyzing'
    PLANNER_PLANNING = 'planner_planning'
    WORKER_EXECUTING = 'worker_executing'
    QA_REVIEWING = 'qa_reviewing'
    CLAUDE_FINAL_VERIFYING = 'claude_final_verifying'
    REVISION_LOOPING = 'revision_looping'
    COMPLETED = 'completed'
    ESCALATED = 'escalated'

class OrchestrationLoop:
    MAX_REVISION_ROUNDS = 3

    def __init__(
        self,
        bus: MessageBus,
        runner: OllamaRunner,
        event_bus: EventBus,
        workspace: WorkspaceManager,
        router: MessageRouter,
    ): ...

    async def run(self, user_instruction: str) -> WorkflowState: ...
    # 현재 상태 조회용 property 필요시 추가
```

server/main.py (현재 — 수정 전):
```python
# lifespan: ollama_runner.start() / stop()
# GET /health
# WebSocket /ws/logs
# ollama_runner 싱글턴 존재
```

server/workspace/manager.py (Phase 1):
```python
class WorkspaceManager:
    def __init__(self, workspace_root: str = 'workspace'): ...
    def write_artifact(self, task_id: str, content: str, filename: str) -> Path: ...
```

agents/ 디렉토리 위치: /Users/johyeonchang/ai-office/agents/
(서버 process의 cwd = /Users/johyeonchang/ai-office/server/ 이므로 ../agents/ 로 접근)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main.py에 OrchestrationLoop 통합 + POST /api/tasks 엔드포인트 추가</name>
  <files>server/main.py</files>
  <read_first>
    - server/main.py (현재 내용 전체 확인 — lifespan, 기존 endpoint 파악)
    - server/orchestration/loop.py (OrchestrationLoop 생성자 파라미터 확인)
    - server/orchestration/router.py (MessageRouter 생성자 확인)
    - server/workspace/manager.py (WorkspaceManager 생성자 확인)
    - server/bus/message_bus.py (MessageBus 생성자 확인)
  </read_first>
  <action>
    server/main.py를 수정하여 다음을 추가한다. 기존 코드(lifespan, health, WebSocket)는 유지.

    **신규 import 추가:**
    ```python
    import asyncio
    import uuid
    from pydantic import BaseModel
    from fastapi import BackgroundTasks
    from bus.message_bus import MessageBus
    from orchestration.loop import OrchestrationLoop, WorkflowState
    from orchestration.router import MessageRouter
    from workspace.manager import WorkspaceManager
    ```

    **싱글턴 인스턴스 추가 (lifespan 수정):**
    ```python
    # 기존 ollama_runner = OllamaRunner() 아래에 추가
    message_bus = MessageBus(db_path='data/bus.db')
    workspace = WorkspaceManager(workspace_root='workspace')
    ```

    lifespan 함수 내 startup 블록에서 MessageRouter와 OrchestrationLoop 생성:
    ```python
    @asynccontextmanager
    async def lifespan(app: FastAPI):
        await ollama_runner.start()
        router = MessageRouter(bus=message_bus, event_bus=event_bus)
        app.state.orch_loop = OrchestrationLoop(
            bus=message_bus,
            runner=ollama_runner,
            event_bus=event_bus,
            workspace=workspace,
            router=router,
        )
        app.state.active_tasks: dict[str, WorkflowState] = {}
        yield
        await ollama_runner.stop()
        message_bus.close()
    ```

    **요청/응답 Pydantic 모델 추가:**
    ```python
    class TaskRequest(BaseModel):
        instruction: str  # 사용자 지시 텍스트

    class TaskResponse(BaseModel):
        task_id: str
        status: str  # 'accepted'
    ```

    **POST /api/tasks 엔드포인트:**
    ```python
    @app.post('/api/tasks', response_model=TaskResponse, status_code=202)
    async def create_task(body: TaskRequest, request: Request):
        '''사용자 지시를 받아 오케스트레이션을 백그라운드로 시작한다 (ORCH-01)'''
        task_id = str(uuid.uuid4())
        loop: OrchestrationLoop = request.app.state.orch_loop
        request.app.state.active_tasks[task_id] = WorkflowState.IDLE

        async def _run():
            final_state = await loop.run(body.instruction)
            request.app.state.active_tasks[task_id] = final_state

        asyncio.create_task(_run())
        return TaskResponse(task_id=task_id, status='accepted')
    ```

    **GET /api/tasks/{task_id} 엔드포인트:**
    ```python
    @app.get('/api/tasks/{task_id}')
    async def get_task_status(task_id: str, request: Request):
        '''태스크 현재 상태를 반환한다'''
        tasks = request.app.state.active_tasks
        if task_id not in tasks:
            from fastapi import HTTPException
            raise HTTPException(status_code=404, detail='태스크를 찾을 수 없습니다')
        return {'task_id': task_id, 'state': tasks[task_id]}
    ```

    **주의:**
    - BackgroundTasks 대신 asyncio.create_task 사용 — OrchestrationLoop.run()이 async이므로 FastAPI BackgroundTasks는 동기 함수 기반이라 부적합
    - Request import: `from fastapi import FastAPI, WebSocket, WebSocketDisconnect, Request`
    - data/ 디렉토리가 없으면 MessageBus 생성 전 mkdir 필요:
      ```python
      from pathlib import Path
      Path('data').mkdir(exist_ok=True)
      ```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run python -c "from main import app; routes = [r.path for r in app.routes]; assert '/api/tasks' in routes, f'엔드포인트 없음: {routes}'; print('OK:', routes)"</automated>
  </verify>
  <acceptance_criteria>
    - `from main import app` import 성공 (구문 에러 없음)
    - app.routes에 POST /api/tasks 존재 (python -c 확인)
    - app.routes에 GET /api/tasks/{task_id} 존재
    - lifespan 내 OrchestrationLoop 생성 코드 존재 (grep: `OrchestrationLoop\(`)
    - asyncio.create_task 사용 (grep: `create_task`)
    - 기존 /health 엔드포인트 유지 (grep: `@app.get.*health`)
    - 기존 /ws/logs WebSocket 유지 (grep: `@app.websocket.*ws/logs`)
  </acceptance_criteria>
  <done>
    main.py 수정 완료, POST /api/tasks 및 GET /api/tasks/{task_id} 존재, 기존 엔드포인트 유지.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_agents.py stub → GREEN 전환 + 전체 통합 검증</name>
  <files>server/tests/test_agents.py</files>
  <read_first>
    - server/tests/test_agents.py (현재 stub 내용 확인)
    - agents/planner.md, agents/designer.md, agents/developer.md, agents/qa.md (Plan 01 산출물 확인)
  </read_first>
  <behavior>
    - test_planner_has_system_prompt: Path('../agents/planner.md').exists() == True 이고 파일 내용에 '역할 정의' 포함
    - test_all_agents_have_system_prompts: planner, designer, developer, qa 4개 파일 모두 존재
    - test_each_agent_has_required_sections: 각 파일에 'JSON 출력', '협업 규칙', '금지사항' 섹션 포함
    - test_qa_prompt_has_independence_rule: agents/qa.md에 '원본 요구사항' 문자열 포함 (D-08 확증편향 방지)
  </behavior>
  <action>
    server/tests/test_agents.py의 xfail stub을 실제 구현 테스트로 전환한다.

    경로 계산: 테스트는 server/ 디렉토리에서 실행되므로 agents/ 파일 경로는:
    ```python
    from pathlib import Path
    AGENTS_DIR = Path(__file__).parent.parent.parent / 'agents'
    # 즉 /Users/johyeonchang/ai-office/agents/
    ```

    구현:
    ```python
    def test_planner_has_system_prompt():
        '''기획자 시스템 프롬프트 파일 존재 및 역할 정의 섹션 포함 (ORCH-02)'''
        planner_path = AGENTS_DIR / 'planner.md'
        assert planner_path.exists(), f'파일 없음: {planner_path}'
        content = planner_path.read_text(encoding='utf-8')
        assert '역할 정의' in content

    def test_all_agents_have_system_prompts():
        '''4개 에이전트 모두 시스템 프롬프트 파일 존재 (ORCH-02)'''
        for agent in ['planner', 'designer', 'developer', 'qa']:
            path = AGENTS_DIR / f'{agent}.md'
            assert path.exists(), f'{agent} 프롬프트 파일 없음: {path}'

    def test_each_agent_has_required_sections():
        '''각 에이전트 프롬프트에 필수 4개 섹션 포함 (D-02)'''
        required = ['역할 정의', 'JSON 출력', '협업 규칙', '금지사항']
        for agent in ['planner', 'designer', 'developer', 'qa']:
            content = (AGENTS_DIR / f'{agent}.md').read_text(encoding='utf-8')
            for section in required:
                assert section in content, f'{agent}.md에 {section!r} 섹션 없음'

    def test_qa_prompt_has_independence_rule():
        '''QA 프롬프트에 원본 요구사항 독립 참조 규칙 포함 (D-08)'''
        content = (AGENTS_DIR / 'qa.md').read_text(encoding='utf-8')
        assert '원본 요구사항' in content, 'QA 확증편향 방지 규칙 누락'
    ```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_agents.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_agents.py` 전체 PASSED (xfail 없음)
    - AGENTS_DIR 경로 계산이 절대 경로로 올바름 (Path(__file__).parent.parent.parent / 'agents')
    - 4개 에이전트 파일 존재 확인 테스트 GREEN
    - QA 독립 참조 규칙 테스트 GREEN
  </acceptance_criteria>
  <done>
    test_agents.py 전체 GREEN.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase 2 전체 통합 검증</name>
  <what-built>
    Phase 2 Orchestration &amp; Workflow 전체 구현:
    - Plan 01: payload 스키마 + 에이전트 시스템 프롬프트 4개 + 테스트 stub 8개
    - Plan 02: TaskGraph (DAG) + MessageRouter (broadcast 복사)
    - Plan 03: OrchestrationLoop (상태 머신, MAX 3회 보완 루프)
    - Plan 04: POST /api/tasks FastAPI 엔드포인트 통합
  </what-built>
  <how-to-verify>
    1. 전체 테스트 suite 실행:
       ```
       cd /Users/johyeonchang/ai-office/server
       uv run pytest --tb=short -q
       ```
       기대: 모든 테스트 PASSED 또는 XFAIL (FAIL 없음)

    2. 서버 실행 확인:
       ```
       cd /Users/johyeonchang/ai-office/server
       uv run uvicorn main:app --reload &
       sleep 3
       curl http://localhost:8000/health
       ```
       기대: {"status": "ok", ...}

    3. POST /api/tasks 엔드포인트 확인:
       ```
       curl -X POST http://localhost:8000/api/tasks \
         -H 'Content-Type: application/json' \
         -d '{"instruction": "간단한 홈페이지를 만들어줘"}'
       ```
       기대: {"task_id": "...(uuid)...", "status": "accepted"} 응답

    4. 에이전트 프롬프트 파일 확인:
       ```
       ls /Users/johyeonchang/ai-office/agents/
       ```
       기대: planner.md, designer.md, developer.md, qa.md 4개 파일 존재
  </how-to-verify>
  <files>N/A — 자동화 작업 없음, 사용자 검증 단계</files>
  <action>자동화 없음 — 위 how-to-verify 절차를 따라 사용자가 직접 검증한다</action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest --tb=short -q</automated>
  </verify>
  <done>전체 테스트 통과 + /api/tasks 엔드포인트 202 응답 확인</done>
  <resume-signal>
    테스트 전체 통과 및 /api/tasks 엔드포인트 응답 확인 후 "approved" 입력.
    이슈 발견 시 구체적 문제 내용 설명.
  </resume-signal>
</task>

</tasks>

<verification>
전체 Phase 2 최종 검증:
cd /Users/johyeonchang/ai-office/server && uv run pytest --tb=short

엔드포인트 라우트 확인:
cd /Users/johyeonchang/ai-office/server && uv run python -c "from main import app; [print(r.path, r.methods) for r in app.routes if hasattr(r, 'methods')]"
</verification>

<success_criteria>
- server/main.py에 POST /api/tasks (202 Accepted) + GET /api/tasks/{task_id} 존재
- OrchestrationLoop가 lifespan에서 MessageBus, OllamaRunner, EventBus, WorkspaceManager, MessageRouter와 함께 생성됨
- test_agents.py 전체 GREEN (에이전트 프롬프트 파일 4개 존재 및 섹션 완비 확인)
- 전체 테스트 suite (Phase 1 + Phase 2) 통과
- 사용자 checkpoint 승인
</success_criteria>

<output>
완료 후 `.planning/phases/02-orchestration-workflow/02-04-SUMMARY.md` 를 생성한다.
</output>
