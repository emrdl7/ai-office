---
phase: 01-infra-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-03
files_modified:
  - server/runners/ollama_runner.py
  - server/tests/test_ollama_runner.py
autonomous: true
requirements:
  - INFR-03

must_haves:
  truths:
    - 'OllamaRunner가 asyncio.Queue 기반 단일 워커로 요청을 순차 처리한다'
    - '동시에 여러 generate() 호출이 들어와도 한 번에 하나씩 Ollama HTTP API로 전달된다'
    - 'Ollama /api/generate에 format: json 파라미터로 호출하여 구조화 출력을 요청한다'
    - 'start()/stop() 생명주기 메서드가 FastAPI lifespan과 연동된다'
  artifacts:
    - path: 'server/runners/ollama_runner.py'
      provides: 'OllamaRunner 클래스 — asyncio.Queue 단일 워커'
      contains: 'asyncio.Queue'
    - path: 'server/tests/test_ollama_runner.py'
      provides: 'INFR-03 검증 테스트'
  key_links:
    - from: 'server/runners/ollama_runner.py'
      to: 'http://localhost:11434/api/generate'
      via: 'httpx.AsyncClient.post()'
      pattern: 'httpx.AsyncClient'
    - from: 'server/runners/json_parser.py'
      to: 'server/runners/ollama_runner.py'
      via: 'generate() 결과 파싱에 parse_json 사용'
      pattern: 'from.*json_parser import parse_json'
---

<objective>
Ollama/Gemma4 HTTP 클라이언트를 asyncio.Queue 기반 단일 워커 패턴으로 구현한다. 동시 요청이 직렬화되어 gemma4:26b 메모리 스래싱이 방지된다.

Purpose: INFR-03 — Gemma4 에이전트들이 단일 큐를 통해 순차적으로 Ollama를 사용하여 로컬 하드웨어 부하를 제어한다.
Output: OllamaRunner 클래스 + 통과하는 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infra-foundation/01-CONTEXT.md
@.planning/phases/01-infra-foundation/01-RESEARCH.md
@.planning/phases/01-infra-foundation/01-01-SUMMARY.md
@.planning/phases/01-infra-foundation/01-03-SUMMARY.md

<interfaces>
<!-- 이 플랜이 사용하는 기존 인터페이스 -->

server/runners/json_parser.py (01-03-PLAN에서 구현):
```python
def parse_json(text: str) -> Any | None:
    '''2-pass JSON 파싱: strict → repair. 복구 불가시 None 반환'''
```

Ollama REST API:
  POST http://localhost:11434/api/generate
  Request: {'model': str, 'prompt': str, 'format': 'json', 'stream': False}
  Response: {'response': str, ...}
  Timeout: 120초 (gemma4:26b 초기 로딩 고려)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OllamaRunner asyncio.Queue 단일 워커 구현</name>
  <files>
    server/runners/ollama_runner.py
    server/tests/test_ollama_runner.py
  </files>
  <read_first>
    - server/runners/ollama_runner.py (현재 stub 내용)
    - server/tests/test_ollama_runner.py (현재 stub 테스트)
    - .planning/phases/01-infra-foundation/01-RESEARCH.md (Pattern 3: Ollama 단일 요청 큐 코드 예시, Pitfall 2: 메모리 스래싱)
    - server/runners/json_parser.py (parse_json 함수 시그니처)
  </read_first>
  <behavior>
    - OllamaRunner(model) 초기화 시 asyncio.Queue와 httpx.AsyncClient를 생성한다
    - start()를 호출하면 단일 _worker coroutine이 asyncio.Task로 시작된다
    - stop()을 호출하면 큐가 비워지고 httpx 클라이언트가 닫히고 worker가 취소된다
    - generate(prompt)는 큐에 (prompt, Future)를 추가하고 Future가 완료될 때까지 대기한다
    - _worker는 큐에서 하나씩 꺼내 Ollama POST /api/generate를 호출한다
    - _call_ollama는 format: json 파라미터로 호출하고 response['response']를 반환한다
    - _call_ollama 실패 시 Future에 예외를 설정한다
    - 동시에 3개의 generate() 호출이 들어와도 Ollama 실제 호출은 순차적이다
  </behavior>
  <action>
**server/runners/ollama_runner.py** 를 다음 내용으로 완전히 교체한다:

```python
# Ollama HTTP 클라이언트 + 단일 요청 큐 (INFR-03)
# asyncio.Queue 단일 워커로 gemma4:26b 메모리 스래싱 방지 (Pitfall 2)
import asyncio
import httpx
from typing import Any

from .json_parser import parse_json

OLLAMA_BASE_URL = 'http://localhost:11434'
DEFAULT_MODEL = 'gemma4:26b'
REQUEST_TIMEOUT = 120.0  # gemma4:26b 초기 로딩 고려


class OllamaRunnerError(Exception):
    '''Ollama HTTP 호출 실패'''
    pass


class OllamaRunner:
    '''asyncio.Queue 기반 단일 워커 Ollama 클라이언트.
    
    모든 generate() 요청은 큐에 쌓이고 단일 워커가 순차 처리한다.
    이를 통해 gemma4:26b의 메모리 압박으로 인한 모델 스래싱을 방지한다.
    
    사용 예시 (FastAPI lifespan):
        runner = OllamaRunner()
        await runner.start()   # lifespan startup
        result = await runner.generate("설계 문서를 작성해줘")
        await runner.stop()    # lifespan shutdown
    '''

    def __init__(
        self,
        model: str = DEFAULT_MODEL,
        base_url: str = OLLAMA_BASE_URL,
        timeout: float = REQUEST_TIMEOUT,
    ):
        self.model = model
        self._base_url = base_url
        self._timeout = timeout
        self._queue: asyncio.Queue = asyncio.Queue()
        self._client: httpx.AsyncClient | None = None
        self._worker_task: asyncio.Task | None = None

    async def start(self) -> None:
        '''워커 시작. FastAPI lifespan startup에서 호출.'''
        self._client = httpx.AsyncClient(
            base_url=self._base_url,
            timeout=self._timeout,
        )
        self._worker_task = asyncio.create_task(self._worker())

    async def stop(self) -> None:
        '''워커 종료. FastAPI lifespan shutdown에서 호출.'''
        if self._worker_task:
            self._worker_task.cancel()
            try:
                await self._worker_task
            except asyncio.CancelledError:
                pass
        if self._client:
            await self._client.aclose()

    async def _worker(self) -> None:
        '''단일 워커 — 큐에서 하나씩 처리하여 순차 처리 보장'''
        while True:
            prompt, future = await self._queue.get()
            try:
                result = await self._call_ollama(prompt)
                if not future.done():
                    future.set_result(result)
            except Exception as exc:
                if not future.done():
                    future.set_exception(exc)
            finally:
                self._queue.task_done()

    async def _call_ollama(self, prompt: str) -> str:
        '''Ollama /api/generate 호출.
        
        format=json으로 구조화 출력 요청.
        응답의 'response' 필드 반환.
        '''
        if not self._client:
            raise OllamaRunnerError('OllamaRunner가 시작되지 않음. start()를 먼저 호출하세요.')

        response = await self._client.post(
            '/api/generate',
            json={
                'model': self.model,
                'prompt': prompt,
                'format': 'json',   # Gemma4 구조화 출력 활성화
                'stream': False,
            },
        )
        response.raise_for_status()
        data = response.json()
        return data['response']

    async def generate(self, prompt: str) -> str:
        '''큐에 요청 추가 후 결과 대기.
        
        동시 호출 시 순차 처리됨 (단일 워커 보장).
        반환값은 Ollama raw 응답 문자열 (JSON 파싱은 호출자 책임).
        '''
        loop = asyncio.get_event_loop()
        future: asyncio.Future = loop.create_future()
        await self._queue.put((prompt, future))
        return await future

    async def generate_json(self, prompt: str) -> Any | None:
        '''generate() + parse_json() 파이프라인.
        
        Gemma4 JSON 출력을 2-pass 파싱하여 Python 객체로 반환.
        복구 불가 시 None 반환.
        '''
        raw = await self.generate(prompt)
        return parse_json(raw)
```

**server/tests/test_ollama_runner.py** 를 다음 내용으로 교체한다:

```python
# INFR-03: OllamaRunner 단일 큐 순차 처리 테스트
# 실제 Ollama 호출은 로컬 환경 의존 → httpx mock 사용
import asyncio
import json
import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from runners.ollama_runner import OllamaRunner, OllamaRunnerError


def _make_ollama_response(text: str) -> dict:
    return {'model': 'gemma4:26b', 'response': text, 'done': True}


@pytest.fixture
async def runner(monkeypatch):
    '''httpx를 mock한 OllamaRunner (start/stop 포함)'''
    call_order: list[str] = []

    async def fake_post(url, **kwargs):
        prompt = kwargs.get('json', {}).get('prompt', '')
        call_order.append(prompt)
        resp = MagicMock()
        resp.raise_for_status = MagicMock()
        resp.json = MagicMock(return_value=_make_ollama_response(f'응답:{prompt}'))
        return resp

    r = OllamaRunner(model='gemma4:26b')
    await r.start()
    r._client.post = fake_post  # type: ignore
    r._call_order = call_order
    yield r
    await r.stop()


async def test_generate_returns_response(runner):
    '''generate()가 Ollama 응답 텍스트를 반환 (INFR-03)'''
    result = await runner.generate('테스트 프롬프트')
    assert '응답:테스트 프롬프트' == result


async def test_sequential_queue_ordering(monkeypatch):
    '''동시 요청이 순차적으로 처리됨 (INFR-03 단일 워커)'''
    call_order: list[str] = []
    call_lock = asyncio.Lock()

    async def fake_post_with_delay(url, **kwargs):
        prompt = kwargs.get('json', {}).get('prompt', '')
        # 처리 순서 기록 (락으로 리스트 안전 접근)
        async with call_lock:
            call_order.append(prompt)
        resp = MagicMock()
        resp.raise_for_status = MagicMock()
        resp.json = MagicMock(return_value=_make_ollama_response(f'done:{prompt}'))
        return resp

    r = OllamaRunner()
    await r.start()
    r._client.post = fake_post_with_delay  # type: ignore

    try:
        # 동시에 3개 요청 발행
        results = await asyncio.gather(
            r.generate('A'),
            r.generate('B'),
            r.generate('C'),
        )
    finally:
        await r.stop()

    # 모든 요청이 처리됨
    assert len(call_order) == 3
    assert set(call_order) == {'A', 'B', 'C'}
    # 결과가 요청에 대응됨
    assert 'done:A' in results
    assert 'done:B' in results
    assert 'done:C' in results


async def test_format_json_param_sent(monkeypatch):
    '''Ollama POST 요청에 format: json 파라미터가 포함됨 (INFR-05 연동)'''
    captured_payload: list[dict] = []

    async def capture_post(url, **kwargs):
        captured_payload.append(kwargs.get('json', {}))
        resp = MagicMock()
        resp.raise_for_status = MagicMock()
        resp.json = MagicMock(return_value=_make_ollama_response('{}'))
        return resp

    r = OllamaRunner()
    await r.start()
    r._client.post = capture_post  # type: ignore

    try:
        await r.generate('테스트')
    finally:
        await r.stop()

    assert len(captured_payload) == 1
    assert captured_payload[0].get('format') == 'json'
    assert captured_payload[0].get('stream') is False


async def test_generate_json_parses_response(monkeypatch):
    '''generate_json()이 JSON 응답을 Python 객체로 반환'''
    async def fake_post(url, **kwargs):
        resp = MagicMock()
        resp.raise_for_status = MagicMock()
        resp.json = MagicMock(return_value=_make_ollama_response('{"status": "done"}'))
        return resp

    r = OllamaRunner()
    await r.start()
    r._client.post = fake_post  # type: ignore

    try:
        result = await r.generate_json('테스트')
    finally:
        await r.stop()

    assert result == {'status': 'done'}
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_ollama_runner.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server/runners/ollama_runner.py에 `class OllamaRunner` 포함
    - server/runners/ollama_runner.py에 `asyncio.Queue` 포함
    - server/runners/ollama_runner.py에 `'format': 'json'` 포함
    - server/runners/ollama_runner.py에 `async def _worker` 포함
    - server/runners/ollama_runner.py에 `from .json_parser import parse_json` 포함
    - `uv run pytest tests/test_ollama_runner.py -v` 가 4개 테스트 모두 PASSED로 종료
    - test_ollama_runner.py에 `xfail` 마커가 없음
  </acceptance_criteria>
  <done>test_ollama_runner.py 4개 테스트 PASSED, 단일 워커 순차 처리 + format:json 파라미터 확인</done>
</task>

</tasks>

<verification>
```bash
cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_ollama_runner.py -v --tb=short
```
4개 테스트가 모두 PASSED여야 한다.
</verification>

<success_criteria>
- test_generate_returns_response PASSED
- test_sequential_queue_ordering PASSED
- test_format_json_param_sent PASSED
- test_generate_json_parses_response PASSED
- server/runners/ollama_runner.py에 asyncio.Queue, 단일 _worker, format:json 모두 포함
</success_criteria>

<output>
완료 후 `.planning/phases/01-infra-foundation/01-05-SUMMARY.md` 생성
</output>
