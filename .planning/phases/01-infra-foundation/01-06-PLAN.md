---
phase: 01-infra-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-04
  - 01-05
files_modified:
  - server/log_bus/event_bus.py
  - server/main.py
  - server/tests/test_log_bus.py
autonomous: true
requirements:
  - INFR-04

must_haves:
  truths:
    - '에이전트 이벤트를 publish()하면 subscribe()한 큐에 이벤트가 도달한다'
    - 'unsubscribe() 후에는 이벤트가 전달되지 않는다'
    - '느린 구독자의 큐가 꽉 차면 해당 큐만 드롭되고 버스는 블록되지 않는다'
    - 'FastAPI /ws/logs WebSocket에 연결하면 이후 발행된 이벤트를 JSON으로 수신한다'
    - 'WebSocket 연결 종료 시 구독자가 자동 정리된다 (메모리 누수 방지)'
  artifacts:
    - path: 'server/log_bus/event_bus.py'
      provides: 'EventBus 클래스 — subscribe/unsubscribe/publish'
      exports: ['EventBus', 'LogEvent']
    - path: 'server/main.py'
      provides: 'FastAPI 앱 + /ws/logs WebSocket 엔드포인트'
      contains: '/ws/logs'
    - path: 'server/tests/test_log_bus.py'
      provides: 'INFR-04 검증 테스트'
  key_links:
    - from: 'server/main.py'
      to: 'server/log_bus/event_bus.py'
      via: 'event_bus = EventBus() 싱글턴 + WebSocket 핸들러'
      pattern: 'event_bus.subscribe'
    - from: 'WebSocket client'
      to: 'server/main.py /ws/logs'
      via: 'ws.send_json(asdict(event))'
      pattern: 'ws.send_json'
---

<objective>
asyncio.Queue 기반 인-프로세스 이벤트 버스를 구현하고 FastAPI WebSocket 엔드포인트에 연결한다. 에이전트가 이벤트를 발행하면 폴링 없이 연결된 대시보드 클라이언트에 즉시 브로드캐스트된다.

Purpose: INFR-04 — Phase 4 대시보드가 WebSocket으로 실시간 에이전트 로그를 수신하는 기반.
Output: EventBus + FastAPI /ws/logs 엔드포인트 + 통과하는 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infra-foundation/01-CONTEXT.md
@.planning/phases/01-infra-foundation/01-RESEARCH.md
@.planning/phases/01-infra-foundation/01-01-SUMMARY.md
@.planning/phases/01-infra-foundation/01-02-SUMMARY.md
@.planning/phases/01-infra-foundation/01-04-SUMMARY.md
@.planning/phases/01-infra-foundation/01-05-SUMMARY.md

<interfaces>
<!-- 이 플랜이 생성하는 인터페이스 (Phase 4에서 사용) -->
<!--
WebSocket 엔드포인트: ws://localhost:8000/ws/logs
메시지 형식 (LogEvent를 dict로 직렬화):
{
  "id": "uuid-string",
  "agent_id": "planner",
  "event_type": "task_start",  // log | status_change | task_start | task_done | error
  "message": "태스크 시작: 홈페이지 기획",
  "data": {},
  "timestamp": "2026-04-03T04:00:00.000000"
}
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EventBus subscribe/publish/unsubscribe 구현</name>
  <files>
    server/log_bus/event_bus.py
    server/tests/test_log_bus.py
  </files>
  <read_first>
    - server/log_bus/event_bus.py (현재 stub 내용)
    - server/tests/test_log_bus.py (현재 stub 테스트)
    - .planning/phases/01-infra-foundation/01-RESEARCH.md (Pattern 4: asyncio.Queue 기반 로그 버스 코드 예시, Pitfall 4: 구독자 메모리 누수)
  </read_first>
  <behavior>
    - EventBus()는 빈 구독자 목록으로 초기화된다
    - subscribe()는 maxsize=500의 새 asyncio.Queue를 구독자 목록에 추가하고 반환한다
    - unsubscribe(q)는 구독자 목록에서 해당 큐를 제거한다
    - publish(event)는 모든 구독자 큐에 put_nowait으로 이벤트를 넣는다
    - 구독자 큐가 꽉 찬 경우 QueueFull을 무시한다 (버스 블록 방지)
    - publish는 async def다 (async for 호환, 향후 DB 로깅 추가 용이)
    - LogEvent는 id(uuid), agent_id, event_type, message, data, timestamp 필드를 가진다
  </behavior>
  <action>
**server/log_bus/event_bus.py** 를 다음 내용으로 완전히 교체한다:

```python
# asyncio.Queue 기반 인-프로세스 이벤트 버스 (INFR-04)
# 결정 근거: SQLite 기반 로그 버스 대신 in-process asyncio.Queue 선택
# 이유: 폴링 레이어 없이 즉시 WebSocket 팬아웃 가능, FastAPI와 동일 프로세스
import asyncio
import uuid
from dataclasses import asdict, dataclass, field
from datetime import datetime
from typing import Any


@dataclass
class LogEvent:
    '''에이전트 이벤트 레코드.
    
    event_type 값:
      'log'           — 일반 로그 메시지
      'status_change' — 에이전트 상태 변경 (작업중/대기/완료/에러)
      'task_start'    — 태스크 시작
      'task_done'     — 태스크 완료
      'error'         — 에러 발생
    '''
    agent_id: str
    event_type: str
    message: str
    data: dict[str, Any] = field(default_factory=dict)
    id: str = field(default_factory=lambda: str(uuid.uuid4()))
    timestamp: str = field(default_factory=lambda: datetime.utcnow().isoformat())

    def to_dict(self) -> dict[str, Any]:
        return asdict(self)


class EventBus:
    '''인-프로세스 pub/sub 이벤트 버스.
    
    구독자는 asyncio.Queue를 받아 이벤트를 소비한다.
    WebSocket 핸들러는 subscribe() → 이벤트 대기 → unsubscribe() 패턴으로 사용한다.
    
    주의사항 (Pitfall 4):
      WebSocket 연결 종료 시 반드시 finally 블록에서 unsubscribe() 호출.
      미호출 시 닫힌 큐에 계속 put_nowait 시도로 메모리 누수 발생.
    '''

    def __init__(self):
        self._subscribers: list[asyncio.Queue] = []

    def subscribe(self) -> asyncio.Queue:
        '''새 구독자 큐 등록. WebSocket 연결 시 호출.
        
        Returns:
            maxsize=500의 asyncio.Queue (이벤트를 소비할 큐)
        '''
        q: asyncio.Queue = asyncio.Queue(maxsize=500)
        self._subscribers.append(q)
        return q

    def unsubscribe(self, q: asyncio.Queue) -> None:
        '''구독자 큐 제거. WebSocket 연결 종료 시 finally에서 호출.
        
        큐가 목록에 없어도 에러 없이 무시한다.
        '''
        try:
            self._subscribers.remove(q)
        except ValueError:
            pass  # 이미 제거된 경우 무시

    async def publish(self, event: LogEvent) -> None:
        '''이벤트를 모든 구독자에게 브로드캐스트.
        
        느린 구독자의 큐가 꽉 찬 경우 해당 구독자만 드롭하고 버스는 계속 동작.
        '''
        for q in list(self._subscribers):  # 복사본으로 순회 (동시 수정 안전)
            try:
                q.put_nowait(event)
            except asyncio.QueueFull:
                pass  # 느린 클라이언트 드롭, 버스는 블록되지 않음

    @property
    def subscriber_count(self) -> int:
        '''현재 구독자 수 (모니터링용)'''
        return len(self._subscribers)


# FastAPI 앱에서 사용할 싱글턴 인스턴스
# from log_bus.event_bus import event_bus
event_bus = EventBus()
```

**server/tests/test_log_bus.py** 를 다음 내용으로 교체한다:

```python
# INFR-04: asyncio.Queue 이벤트 버스 테스트
import asyncio
import pytest
from log_bus.event_bus import EventBus, LogEvent


@pytest.fixture
def bus():
    return EventBus()


def _make_event(**kwargs) -> LogEvent:
    defaults = {
        'agent_id': 'planner',
        'event_type': 'log',
        'message': '테스트 이벤트',
    }
    defaults.update(kwargs)
    return LogEvent(**defaults)


async def test_publish_reaches_subscriber(bus):
    '''발행된 이벤트가 구독자 큐에 도달함 (INFR-04)'''
    q = bus.subscribe()
    event = _make_event(message='태스크 시작')

    await bus.publish(event)

    received = q.get_nowait()
    assert received.message == '태스크 시작'
    assert received.agent_id == 'planner'


async def test_unsubscribe_stops_delivery(bus):
    '''unsubscribe 후 이벤트가 전달되지 않음 (Pitfall 4 방지)'''
    q = bus.subscribe()
    bus.unsubscribe(q)

    await bus.publish(_make_event())

    assert q.empty()


async def test_multiple_subscribers_all_receive(bus):
    '''다수 구독자가 모두 이벤트를 수신함'''
    q1 = bus.subscribe()
    q2 = bus.subscribe()
    q3 = bus.subscribe()

    await bus.publish(_make_event(message='브로드캐스트'))

    assert not q1.empty()
    assert not q2.empty()
    assert not q3.empty()
    assert q1.get_nowait().message == '브로드캐스트'


async def test_full_queue_does_not_block_bus(bus):
    '''꽉 찬 구독자 큐가 버스를 블록하지 않음 (QueueFull 처리)'''
    # maxsize=1로 작은 큐 생성 후 꽉 채움
    import asyncio
    q_full = asyncio.Queue(maxsize=1)
    q_full.put_nowait(_make_event())  # 큐 꽉 참
    bus._subscribers.append(q_full)

    q_normal = bus.subscribe()

    # publish가 블록되지 않아야 함
    await asyncio.wait_for(bus.publish(_make_event(message='정상 수신')), timeout=1.0)

    # 정상 구독자는 이벤트 수신
    assert not q_normal.empty()
    assert q_normal.get_nowait().message == '정상 수신'


async def test_log_event_has_required_fields(bus):
    '''LogEvent가 필수 필드(id, timestamp)를 자동 생성'''
    event = _make_event()
    assert event.id  # uuid 자동 생성
    assert event.timestamp  # datetime 자동 생성
    assert event.data == {}  # 기본값

    d = event.to_dict()
    assert 'id' in d
    assert 'timestamp' in d
    assert 'agent_id' in d
    assert 'event_type' in d
    assert 'message' in d


async def test_subscriber_count(bus):
    '''subscriber_count가 정확한 수를 반환'''
    assert bus.subscriber_count == 0
    q1 = bus.subscribe()
    q2 = bus.subscribe()
    assert bus.subscriber_count == 2
    bus.unsubscribe(q1)
    assert bus.subscriber_count == 1
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_log_bus.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server/log_bus/event_bus.py에 `class EventBus` 포함
    - server/log_bus/event_bus.py에 `class LogEvent` 포함
    - server/log_bus/event_bus.py에 `maxsize=500` 포함
    - server/log_bus/event_bus.py에 `asyncio.QueueFull` 포함
    - server/log_bus/event_bus.py에 `event_bus = EventBus()` 싱글턴 포함
    - `uv run pytest tests/test_log_bus.py -v` 가 6개 테스트 모두 PASSED로 종료
    - test_log_bus.py에 `xfail` 마커가 없음
  </acceptance_criteria>
  <done>test_log_bus.py 6개 테스트 PASSED, EventBus pub/sub 동작 확인</done>
</task>

<task type="auto">
  <name>Task 2: FastAPI /ws/logs WebSocket 엔드포인트 + lifespan 연동</name>
  <files>
    server/main.py
  </files>
  <read_first>
    - server/main.py (현재 stub 내용 — health 엔드포인트만 있음)
    - server/log_bus/event_bus.py (방금 구현한 EventBus, event_bus 싱글턴)
    - .planning/phases/01-infra-foundation/01-RESEARCH.md (Pattern 4: FastAPI WebSocket 엔드포인트 예시 주석)
  </read_first>
  <action>
server/main.py를 다음 내용으로 완전히 교체한다:

```python
# FastAPI 오케스트레이션 서버 진입점 (INFR-04)
# WebSocket /ws/logs: 에이전트 이벤트 실시간 브로드캐스트
from contextlib import asynccontextmanager
from dataclasses import asdict

from fastapi import FastAPI, WebSocket, WebSocketDisconnect

from log_bus.event_bus import EventBus, LogEvent, event_bus
from runners.ollama_runner import OllamaRunner

# OllamaRunner 싱글턴 (lifespan에서 start/stop)
ollama_runner = OllamaRunner()


@asynccontextmanager
async def lifespan(app: FastAPI):
    '''FastAPI 생명주기 관리.
    
    Startup: OllamaRunner 워커 시작
    Shutdown: OllamaRunner 워커 종료
    '''
    await ollama_runner.start()
    yield
    await ollama_runner.stop()


app = FastAPI(title='AI Office', lifespan=lifespan)


@app.get('/health')
async def health():
    '''서버 상태 확인'''
    return {
        'status': 'ok',
        'log_bus_subscribers': event_bus.subscriber_count,
    }


@app.websocket('/ws/logs')
async def log_stream(ws: WebSocket):
    '''실시간 에이전트 로그 스트림 (INFR-04).
    
    연결 시: event_bus 구독
    이벤트 수신 시: JSON으로 클라이언트에 전송
    연결 종료 시: finally에서 반드시 구독 해제 (Pitfall 4 방지)
    
    메시지 형식:
    {
      "id": "uuid",
      "agent_id": "planner",
      "event_type": "task_start",
      "message": "태스크 시작",
      "data": {},
      "timestamp": "2026-04-03T..."
    }
    '''
    await ws.accept()
    q = event_bus.subscribe()
    try:
        while True:
            event: LogEvent = await q.get()
            await ws.send_json(asdict(event))
    except WebSocketDisconnect:
        pass
    finally:
        event_bus.unsubscribe(q)  # 반드시 구독 해제 (메모리 누수 방지)
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run python -c "from main import app; routes = [r.path for r in app.routes]; assert '/health' in routes and '/ws/logs' in routes, f'경로 누락: {routes}'; print('routes OK:', routes)"</automated>
  </verify>
  <acceptance_criteria>
    - server/main.py에 `@app.websocket('/ws/logs')` 포함
    - server/main.py에 `event_bus.subscribe()` 포함
    - server/main.py에 `event_bus.unsubscribe(q)` 포함 (finally 블록)
    - server/main.py에 `await ws.send_json` 포함
    - server/main.py에 `lifespan` 함수 존재
    - server/main.py에 `ollama_runner.start()` 포함
    - `uv run python -c "from main import app; ..."` 가 'routes OK' 출력하며 0 exit code로 종료
  </acceptance_criteria>
  <done>FastAPI 앱이 /health, /ws/logs 라우트를 가지며, WebSocket 연결 시 이벤트 버스 구독/해제가 동작함</done>
</task>

</tasks>

<verification>
```bash
# 이벤트 버스 테스트
cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_log_bus.py -v --tb=short

# FastAPI 라우트 확인
cd /Users/johyeonchang/ai-office/server && uv run python -c "from main import app; print([r.path for r in app.routes])"

# 전체 테스트 스위트
cd /Users/johyeonchang/ai-office/server && uv run pytest --tb=short -q
```
</verification>

<success_criteria>
- test_log_bus.py 6개 테스트 PASSED
- FastAPI 앱이 /health, /ws/logs 라우트 포함
- 전체 테스트 스위트: test_message_bus(5), test_workspace(7), test_json_parser(8), test_claude_runner(5), test_ollama_runner(4), test_log_bus(6) = 35개 PASSED (xfail이었던 테스트는 해당 플랜에서 모두 실제 테스트로 교체됨)
- server/main.py에 event_bus.unsubscribe(q)가 finally 블록에 존재 (메모리 누수 방지)
</success_criteria>

<output>
완료 후 `.planning/phases/01-infra-foundation/01-06-SUMMARY.md` 생성
</output>
