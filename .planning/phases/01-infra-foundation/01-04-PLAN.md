---
phase: 01-infra-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
files_modified:
  - server/runners/claude_runner.py
  - server/tests/test_claude_runner.py
autonomous: true
requirements:
  - INFR-02

must_haves:
  truths:
    - 'Claude CLI subprocess가 --bare --print --output-format stream-json --no-session-persistence 플래그로 실행된다'
    - 'subprocess 결과가 JSON-lines 스트림으로 파싱되어 텍스트로 반환된다'
    - '격리 디렉토리(/tmp/ai-office-claude-isolated/)에서 실행되어 CLAUDE.md 자동 탐색이 차단된다'
    - '토큰 격리: 불필요한 글로벌 설정이 주입되지 않는다'
  artifacts:
    - path: 'server/runners/claude_runner.py'
      provides: 'run_claude_isolated() 비동기 함수'
      contains: '--bare'
    - path: 'server/tests/test_claude_runner.py'
      provides: 'INFR-02 검증 테스트'
  key_links:
    - from: 'server/runners/claude_runner.py'
      to: 'claude CLI'
      via: 'asyncio.create_subprocess_exec'
      pattern: 'asyncio.create_subprocess_exec'
---

<objective>
Claude CLI를 Python subprocess로 실행하는 격리 러너를 구현한다. --bare 플래그로 CLAUDE.md 자동 로드와 MCP 플러그인을 차단하고, JSON-lines 응답을 파싱한다.

Purpose: INFR-02 — Claude가 오케스트레이터 역할로 태스크를 분석·검증할 때 토큰 낭비 없이 실행되어야 한다.
Output: run_claude_isolated() 함수 + 통과하는 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infra-foundation/01-CONTEXT.md
@.planning/phases/01-infra-foundation/01-RESEARCH.md
@.planning/phases/01-infra-foundation/01-01-SUMMARY.md

<interfaces>
<!-- 이 플랜이 참조하는 패턴 -->
<!-- Pattern 2 from RESEARCH.md: Claude CLI subprocess (토큰 격리) -->
<!--
핵심 플래그:
  --bare: CLAUDE.md 자동 로드, MCP, 훅, 플러그인 모두 비활성화
  --print: 비대화형 모드
  --output-format stream-json: JSON-lines 응답
  --no-session-persistence: 세션을 디스크에 저장하지 않음

JSON-lines 파싱:
  각 라인을 json.loads
  type == 'assistant' 이벤트에서 content[].type == 'text' 추출

격리 디렉토리:
  ISOLATION_DIR = Path('/tmp/ai-office-claude-isolated')
  cwd=str(ISOLATION_DIR) 로 subprocess 실행
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Claude CLI subprocess 러너 구현</name>
  <files>
    server/runners/claude_runner.py
    server/tests/test_claude_runner.py
  </files>
  <read_first>
    - server/runners/claude_runner.py (현재 stub 내용)
    - server/tests/test_claude_runner.py (현재 stub 테스트)
    - .planning/phases/01-infra-foundation/01-RESEARCH.md (Pattern 2: Claude CLI subprocess 코드 예시, Pitfall 1: 토큰 폭발)
    - .planning/phases/01-infra-foundation/01-CONTEXT.md (D-05, D-06 결정 사항)
  </read_first>
  <behavior>
    - run_claude_isolated(prompt) 는 str을 반환하는 async 함수다
    - subprocess 명령은 정확히 ['claude', '--bare', '--print', '--output-format', 'stream-json', '--no-session-persistence', prompt] 순서다
    - cwd는 '/tmp/ai-office-claude-isolated' 격리 디렉토리다
    - JSON-lines 응답에서 type='assistant' 이벤트의 content[].type='text' 값을 추출해 연결해서 반환한다
    - 파싱 불가 라인은 무시한다 (continue)
    - subprocess 실패 시 (returncode != 0) ClaudeRunnerError를 발생시킨다
    - stderr를 캡처하며 에러 메시지에 포함한다
  </behavior>
  <action>
**server/runners/claude_runner.py** 를 다음 내용으로 완전히 교체한다:

```python
# Claude CLI subprocess 러너 (INFR-02, D-05, D-06)
# --bare: CLAUDE.md 자동 로드, MCP, 훅, 플러그인 모두 비활성화 → 토큰 격리
# --print: 비대화형 모드 (stdin → stdout → 종료)
# --output-format stream-json: JSON-lines 응답
# --no-session-persistence: 세션을 디스크에 저장하지 않음
import asyncio
import json
from pathlib import Path

# 격리 디렉토리: CLAUDE.md 자동 탐색 차단 (D-06)
ISOLATION_DIR = Path('/tmp/ai-office-claude-isolated')


class ClaudeRunnerError(Exception):
    '''Claude CLI subprocess 실행 실패'''
    pass


async def run_claude_isolated(prompt: str) -> str:
    '''Claude CLI를 격리 subprocess로 실행하고 텍스트 응답을 반환한다.
    
    Args:
        prompt: Claude에게 전달할 프롬프트 텍스트
        
    Returns:
        Claude의 텍스트 응답 (JSON-lines 스트림에서 추출)
        
    Raises:
        ClaudeRunnerError: subprocess가 비정상 종료된 경우
    '''
    ISOLATION_DIR.mkdir(parents=True, exist_ok=True)

    proc = await asyncio.create_subprocess_exec(
        'claude',
        '--bare',
        '--print',
        '--output-format', 'stream-json',
        '--no-session-persistence',
        prompt,
        cwd=str(ISOLATION_DIR),
        stdout=asyncio.subprocess.PIPE,
        stderr=asyncio.subprocess.PIPE,
    )

    stdout, stderr = await proc.communicate()

    if proc.returncode != 0:
        error_msg = stderr.decode(errors='replace').strip()
        raise ClaudeRunnerError(
            f'Claude CLI 실패 (exit {proc.returncode}): {error_msg}'
        )

    result_parts: list[str] = []
    for line in stdout.decode(errors='replace').splitlines():
        line = line.strip()
        if not line:
            continue
        try:
            event = json.loads(line)
            # stream-json 이벤트: type='assistant', content 블록 추출
            if event.get('type') == 'assistant':
                for block in event.get('message', {}).get('content', []):
                    if block.get('type') == 'text':
                        result_parts.append(block['text'])
        except json.JSONDecodeError:
            pass  # 파싱 불가 라인 무시 (스트림 메타데이터 등)

    return ''.join(result_parts)
```

**server/tests/test_claude_runner.py** 를 다음 내용으로 교체한다:

```python
# INFR-02: Claude CLI subprocess 러너 테스트
# 실제 Claude CLI 호출은 로컬 환경 의존 → unittest.mock.AsyncMock 사용
import asyncio
import json
import pytest
from unittest.mock import AsyncMock, MagicMock, patch
from runners.claude_runner import run_claude_isolated, ClaudeRunnerError


def _make_stream_json_output(text: str) -> bytes:
    '''테스트용 stream-json 응답 생성'''
    event = {
        'type': 'assistant',
        'message': {
            'content': [{'type': 'text', 'text': text}]
        }
    }
    return json.dumps(event).encode() + b'\n'


@pytest.fixture
def mock_proc_success(monkeypatch):
    '''성공적인 Claude subprocess mock'''
    async def fake_subprocess(*args, **kwargs):
        proc = AsyncMock()
        proc.returncode = 0
        proc.communicate = AsyncMock(
            return_value=(
                _make_stream_json_output('테스트 응답'),
                b'',
            )
        )
        return proc

    monkeypatch.setattr(
        'runners.claude_runner.asyncio.create_subprocess_exec',
        fake_subprocess,
    )


@pytest.fixture
def mock_proc_failure(monkeypatch):
    '''실패하는 Claude subprocess mock'''
    async def fake_subprocess(*args, **kwargs):
        proc = AsyncMock()
        proc.returncode = 1
        proc.communicate = AsyncMock(
            return_value=(b'', b'Claude CLI error: not found')
        )
        return proc

    monkeypatch.setattr(
        'runners.claude_runner.asyncio.create_subprocess_exec',
        fake_subprocess,
    )


async def test_run_claude_returns_text(mock_proc_success):
    '''Claude subprocess가 텍스트 응답을 반환 (INFR-02)'''
    result = await run_claude_isolated('테스트 프롬프트')
    assert result == '테스트 응답'


async def test_bare_flag_in_subprocess_command(monkeypatch):
    '''subprocess 명령에 --bare 플래그가 포함됨 (D-05, D-06 토큰 격리)'''
    captured_args = []

    async def capture_subprocess(*args, **kwargs):
        captured_args.extend(args)
        proc = AsyncMock()
        proc.returncode = 0
        proc.communicate = AsyncMock(return_value=(b'', b''))
        return proc

    monkeypatch.setattr(
        'runners.claude_runner.asyncio.create_subprocess_exec',
        capture_subprocess,
    )

    await run_claude_isolated('test')

    assert '--bare' in captured_args
    assert '--print' in captured_args
    assert '--output-format' in captured_args
    assert 'stream-json' in captured_args
    assert '--no-session-persistence' in captured_args


async def test_isolation_dir_used_as_cwd(monkeypatch):
    '''격리 디렉토리가 subprocess cwd로 사용됨 (D-06)'''
    captured_kwargs = {}

    async def capture_subprocess(*args, **kwargs):
        captured_kwargs.update(kwargs)
        proc = AsyncMock()
        proc.returncode = 0
        proc.communicate = AsyncMock(return_value=(b'', b''))
        return proc

    monkeypatch.setattr(
        'runners.claude_runner.asyncio.create_subprocess_exec',
        capture_subprocess,
    )

    await run_claude_isolated('test')

    assert 'cwd' in captured_kwargs
    assert 'ai-office-claude-isolated' in captured_kwargs['cwd']


async def test_failure_raises_claude_runner_error(mock_proc_failure):
    '''subprocess 실패 시 ClaudeRunnerError 발생'''
    with pytest.raises(ClaudeRunnerError, match='Claude CLI 실패'):
        await run_claude_isolated('test')


async def test_invalid_json_lines_ignored(monkeypatch):
    '''파싱 불가 JSON-lines는 무시되고 유효한 부분만 반환'''
    async def fake_subprocess(*args, **kwargs):
        # 유효한 stream-json + 파싱 불가 라인 혼합
        output = b'not-json\n' + _make_stream_json_output('유효한 응답')
        proc = AsyncMock()
        proc.returncode = 0
        proc.communicate = AsyncMock(return_value=(output, b''))
        return proc

    monkeypatch.setattr(
        'runners.claude_runner.asyncio.create_subprocess_exec',
        fake_subprocess,
    )

    result = await run_claude_isolated('test')
    assert result == '유효한 응답'
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_claude_runner.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server/runners/claude_runner.py에 `'--bare'` 문자열 포함
    - server/runners/claude_runner.py에 `'--no-session-persistence'` 문자열 포함
    - server/runners/claude_runner.py에 `ISOLATION_DIR` 변수 포함
    - server/runners/claude_runner.py에 `class ClaudeRunnerError` 포함
    - server/runners/claude_runner.py에 `asyncio.create_subprocess_exec` 포함
    - `uv run pytest tests/test_claude_runner.py -v` 가 5개 테스트 모두 PASSED로 종료
    - test_claude_runner.py에 `xfail` 마커가 없음
  </acceptance_criteria>
  <done>test_claude_runner.py 5개 테스트 PASSED, --bare 플래그 토큰 격리 패턴 구현 확인</done>
</task>

</tasks>

<verification>
```bash
cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_claude_runner.py -v --tb=short
```
5개 테스트가 모두 PASSED여야 한다.
</verification>

<success_criteria>
- test_run_claude_returns_text PASSED
- test_bare_flag_in_subprocess_command PASSED
- test_isolation_dir_used_as_cwd PASSED
- test_failure_raises_claude_runner_error PASSED
- test_invalid_json_lines_ignored PASSED
- server/runners/claude_runner.py에 --bare, ISOLATION_DIR, ClaudeRunnerError 모두 포함
</success_criteria>

<output>
완료 후 `.planning/phases/01-infra-foundation/01-04-SUMMARY.md` 생성
</output>
