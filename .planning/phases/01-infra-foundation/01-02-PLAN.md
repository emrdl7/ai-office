---
phase: 01-infra-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - server/db/client.py
  - server/bus/message_bus.py
  - server/tests/test_message_bus.py
autonomous: true
requirements:
  - INFR-01

must_haves:
  truths:
    - '메시지를 발행하면 SQLite 테이블에 저장되고, 소비 호출 시 반환된다'
    - '소비된 메시지에 ACK하면 status가 done으로 변경된다'
    - 'DB 파일을 WAL 모드로 열며 PRAGMA journal_mode=WAL이 적용된다'
    - 'SQLITE_BUSY 에러에 5000ms busy_timeout이 설정된다'
  artifacts:
    - path: 'server/db/client.py'
      provides: 'SQLite WAL 연결 관리'
      contains: 'PRAGMA journal_mode=WAL'
    - path: 'server/bus/message_bus.py'
      provides: 'publish/consume/ack 메서드를 가진 MessageBus 클래스'
      exports: ['MessageBus']
    - path: 'server/tests/test_message_bus.py'
      provides: 'INFR-01 검증 테스트'
  key_links:
    - from: 'server/bus/message_bus.py'
      to: 'server/db/client.py'
      via: 'get_connection() 호출'
      pattern: 'from.*db.*client import get_connection'
    - from: 'server/bus/message_bus.py'
      to: 'server/bus/schemas.py'
      via: 'AgentMessage import'
      pattern: 'from.*schemas import AgentMessage'
---

<objective>
SQLite WAL 모드 기반 메시지 버스를 구현한다. 에이전트 간 메시지 발행(publish), 소비(consume), 확인(ack) 사이클이 완전히 동작하고 테스트로 검증된다.

Purpose: Phase 2에서 오케스트레이션 서버가 에이전트 간 태스크 전달에 사용할 핵심 통신 레이어.
Output: MessageBus 클래스 + SQLite WAL 연결 + 통과하는 테스트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-infra-foundation/01-CONTEXT.md
@.planning/phases/01-infra-foundation/01-RESEARCH.md
@.planning/phases/01-infra-foundation/01-01-SUMMARY.md

<interfaces>
<!-- 이 플랜이 사용하는 기존 인터페이스 -->

server/bus/schemas.py에서 생성된 AgentMessage:
```python
from pydantic import BaseModel, Field
from typing import Literal, Optional, Any
from datetime import datetime
import uuid

MessageType = Literal['task_request', 'task_result', 'status_update']
AgentId = Literal['claude', 'planner', 'developer', 'designer', 'qa', 'orchestrator']
Priority = Literal['normal', 'high', 'urgent']
Status = Literal['pending', 'processing', 'done', 'failed']

class AgentMessage(BaseModel):
    id: str = Field(default_factory=lambda: str(uuid.uuid4()))
    type: MessageType
    from_agent: AgentId = Field(alias='from')
    to_agent: AgentId | Literal['broadcast'] = Field(alias='to')
    payload: Any
    reply_to: Optional[str] = None
    priority: Priority = 'normal'
    tags: list[str] = Field(default_factory=list)
    metadata: dict[str, Any] = Field(default_factory=dict)
    created_at: datetime = Field(default_factory=datetime.utcnow)
    ack_at: Optional[datetime] = None
    status: Status = 'pending'
    model_config = {'populate_by_name': True}
```

tests/conftest.py fixture:
```python
@pytest.fixture
def in_memory_db():  # sqlite3.Connection, ':memory:', WAL 모드
@pytest.fixture
def tmp_workspace(tmp_path):  # Path
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SQLite WAL 연결 관리자 구현</name>
  <files>
    server/db/client.py
  </files>
  <read_first>
    - server/db/client.py (현재 stub 내용 확인)
    - .planning/phases/01-infra-foundation/01-RESEARCH.md (Pattern 1: SQLite WAL 모드 메시지 버스 코드 예시)
  </read_first>
  <behavior>
    - get_connection()은 sqlite3.Connection을 반환한다
    - 반환된 연결에 PRAGMA journal_mode=WAL이 적용되어 있다
    - PRAGMA synchronous=NORMAL이 적용되어 있다
    - PRAGMA foreign_keys=ON이 적용되어 있다
    - PRAGMA busy_timeout=5000이 적용되어 있다
    - conn.row_factory = sqlite3.Row가 설정되어 있다
    - check_same_thread=False로 열린다
  </behavior>
  <action>
server/db/client.py를 다음 내용으로 완전히 교체한다:

```python
# SQLite WAL 모드 연결 관리 (INFR-01)
import sqlite3
from pathlib import Path

DB_PATH = Path('data/bus.db')

def get_connection(db_path: str | Path = DB_PATH) -> sqlite3.Connection:
    '''SQLite WAL 모드 연결 반환.
    
    PRAGMA 설정:
    - journal_mode=WAL: 다중 에이전트 동시 읽기/쓰기 안전
    - synchronous=NORMAL: WAL 모드에서 FULL 대비 2-3x 빠름
    - foreign_keys=ON: 참조 무결성 강제
    - busy_timeout=5000: 5초 대기 후 SQLITE_BUSY 에러 (Pitfall 3 방지)
    '''
    db_path = Path(db_path)
    db_path.parent.mkdir(parents=True, exist_ok=True)
    
    conn = sqlite3.connect(str(db_path), check_same_thread=False)
    conn.execute('PRAGMA journal_mode=WAL')
    conn.execute('PRAGMA synchronous=NORMAL')
    conn.execute('PRAGMA foreign_keys=ON')
    conn.execute('PRAGMA busy_timeout=5000')
    conn.row_factory = sqlite3.Row
    return conn


def init_schema(conn: sqlite3.Connection) -> None:
    '''메시지 버스 테이블 및 인덱스 생성'''
    conn.executescript('''
        CREATE TABLE IF NOT EXISTS messages (
            id          TEXT PRIMARY KEY,
            type        TEXT NOT NULL,
            from_agent  TEXT NOT NULL,
            to_agent    TEXT NOT NULL,
            payload     TEXT NOT NULL,
            reply_to    TEXT,
            priority    TEXT NOT NULL DEFAULT 'normal',
            tags        TEXT NOT NULL DEFAULT '[]',
            metadata    TEXT NOT NULL DEFAULT '{}',
            created_at  TEXT NOT NULL,
            ack_at      TEXT,
            status      TEXT NOT NULL DEFAULT 'pending'
        );

        CREATE INDEX IF NOT EXISTS idx_messages_to_status
            ON messages(to_agent, status, created_at);
    ''')
    conn.commit()
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run python -c "from db.client import get_connection; conn = get_connection(':memory:'); row = conn.execute('PRAGMA journal_mode').fetchone(); print('WAL OK' if row[0] == 'wal' else 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - server/db/client.py에 `PRAGMA journal_mode=WAL` 문자열 포함
    - server/db/client.py에 `PRAGMA busy_timeout=5000` 문자열 포함
    - server/db/client.py에 `PRAGMA synchronous=NORMAL` 문자열 포함
    - server/db/client.py에 `check_same_thread=False` 포함
    - server/db/client.py에 `def init_schema` 함수 존재
    - `get_connection(':memory:')` 호출 후 `PRAGMA journal_mode` 결과가 `wal`
  </acceptance_criteria>
  <done>get_connection()이 WAL 모드 SQLite 연결 반환, init_schema()가 messages 테이블 생성</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MessageBus publish/consume/ack 구현 및 테스트 완성</name>
  <files>
    server/bus/message_bus.py
    server/tests/test_message_bus.py
  </files>
  <read_first>
    - server/bus/message_bus.py (현재 stub 내용)
    - server/tests/test_message_bus.py (현재 stub 테스트)
    - server/bus/schemas.py (AgentMessage 인터페이스 확인)
    - server/db/client.py (방금 구현한 get_connection, init_schema)
    - .planning/phases/01-infra-foundation/01-RESEARCH.md (Pattern 1: SQLite WAL 모드 메시지 버스, Anti-Patterns)
  </read_first>
  <behavior>
    - publish(message): AgentMessage를 DB에 저장, status='pending'
    - consume(to_agent, limit): to_agent로 수신된 status='pending' 메시지를 created_at 오름차순으로 limit개 반환
    - ack(message_id): 해당 메시지의 status를 'done'으로, ack_at을 현재 시각으로 업데이트
    - 발행 후 consume은 같은 메시지를 반환한다
    - ACK 후 consume은 해당 메시지를 반환하지 않는다
    - payload는 JSON 직렬화되어 저장된다
  </behavior>
  <action>
**server/bus/message_bus.py** 를 다음 내용으로 완전히 교체한다:

```python
# SQLite WAL 기반 메시지 버스 (INFR-01)
import json
import sqlite3
from datetime import datetime
from pathlib import Path

from .schemas import AgentMessage
from ..db.client import get_connection, init_schema


class MessageBus:
    def __init__(self, db_path: str | Path = 'data/bus.db'):
        self._conn = get_connection(db_path)
        init_schema(self._conn)

    def publish(self, message: AgentMessage) -> None:
        '''메시지를 버스에 발행 (status=pending)'''
        data = message.model_dump(by_alias=False)
        self._conn.execute(
            '''INSERT INTO messages
               (id, type, from_agent, to_agent, payload, reply_to,
                priority, tags, metadata, created_at, ack_at, status)
               VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)''',
            (
                data['id'],
                data['type'],
                data['from_agent'],
                data['to_agent'],
                json.dumps(data['payload']),
                data['reply_to'],
                data['priority'],
                json.dumps(data['tags']),
                json.dumps(data['metadata']),
                data['created_at'].isoformat() if isinstance(data['created_at'], datetime) else data['created_at'],
                data['ack_at'].isoformat() if data['ack_at'] and isinstance(data['ack_at'], datetime) else data['ack_at'],
                data['status'],
            ),
        )
        self._conn.commit()

    def consume(self, to_agent: str, limit: int = 10) -> list[AgentMessage]:
        '''pending 메시지를 수신 (created_at 오름차순)'''
        rows = self._conn.execute(
            '''SELECT * FROM messages
               WHERE to_agent = ? AND status = 'pending'
               ORDER BY created_at ASC
               LIMIT ?''',
            (to_agent, limit),
        ).fetchall()
        return [self._row_to_message(row) for row in rows]

    def ack(self, message_id: str) -> None:
        '''메시지 처리 완료 표시 (status=done, ack_at=now)'''
        self._conn.execute(
            "UPDATE messages SET status='done', ack_at=? WHERE id=?",
            (datetime.utcnow().isoformat(), message_id),
        )
        self._conn.commit()

    def _row_to_message(self, row: sqlite3.Row) -> AgentMessage:
        return AgentMessage.model_validate({
            'id': row['id'],
            'type': row['type'],
            'from': row['from_agent'],
            'to': row['to_agent'],
            'payload': json.loads(row['payload']),
            'reply_to': row['reply_to'],
            'priority': row['priority'],
            'tags': json.loads(row['tags']),
            'metadata': json.loads(row['metadata']),
            'created_at': row['created_at'],
            'ack_at': row['ack_at'],
            'status': row['status'],
        })

    def close(self):
        self._conn.close()
```

**server/tests/test_message_bus.py** 를 다음 내용으로 교체한다 (xfail 제거, 실제 테스트):

```python
# INFR-01: SQLite WAL 메시지 버스 테스트
import pytest
from bus.message_bus import MessageBus
from bus.schemas import AgentMessage


@pytest.fixture
def bus(tmp_path):
    '''임시 DB 경로를 사용하는 MessageBus'''
    db_path = tmp_path / 'test_bus.db'
    b = MessageBus(db_path=str(db_path))
    yield b
    b.close()


def _make_message(**kwargs) -> AgentMessage:
    defaults = {
        'type': 'task_request',
        'from': 'claude',
        'to': 'planner',
        'payload': {'task': 'test'},
    }
    defaults.update(kwargs)
    return AgentMessage.model_validate(defaults)


def test_publish_and_consume(bus):
    '''메시지 발행 후 소비 왕복 테스트 (INFR-01)'''
    msg = _make_message(payload={'task': 'design homepage'})
    bus.publish(msg)

    consumed = bus.consume(to_agent='planner')

    assert len(consumed) == 1
    assert consumed[0].id == msg.id
    assert consumed[0].payload == {'task': 'design homepage'}


def test_ack_removes_from_pending(bus):
    '''ACK 후 메시지가 pending 소비에서 제거됨 (INFR-01)'''
    msg = _make_message()
    bus.publish(msg)

    before_ack = bus.consume(to_agent='planner')
    assert len(before_ack) == 1

    bus.ack(msg.id)

    after_ack = bus.consume(to_agent='planner')
    assert len(after_ack) == 0


def test_consume_filters_by_recipient(bus):
    '''다른 에이전트의 메시지는 소비되지 않음'''
    msg_planner = _make_message(**{'to': 'planner'})
    msg_developer = _make_message(**{'to': 'developer'})
    bus.publish(msg_planner)
    bus.publish(msg_developer)

    planner_msgs = bus.consume(to_agent='planner')
    developer_msgs = bus.consume(to_agent='developer')

    assert len(planner_msgs) == 1
    assert len(developer_msgs) == 1
    assert planner_msgs[0].id == msg_planner.id


def test_consume_respects_limit(bus):
    '''limit 파라미터로 반환 수 제한'''
    for i in range(5):
        bus.publish(_make_message(payload={'i': i}))

    result = bus.consume(to_agent='planner', limit=2)
    assert len(result) == 2


def test_atomic_write_pattern(bus, tmp_path):
    '''메시지 발행 후 DB 파일이 존재하고 tmp 파일이 없음'''
    import glob
    bus.publish(_make_message())
    tmp_files = glob.glob(str(tmp_path / '*.tmp.*'))
    assert len(tmp_files) == 0, f'임시 파일 잔존: {tmp_files}'
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_message_bus.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - server/bus/message_bus.py에 `class MessageBus` 포함
    - server/bus/message_bus.py에 `def publish` 포함
    - server/bus/message_bus.py에 `def consume` 포함
    - server/bus/message_bus.py에 `def ack` 포함
    - server/bus/message_bus.py에 `json.dumps` 포함 (payload 직렬화)
    - `uv run pytest tests/test_message_bus.py -v` 가 5개 테스트 모두 PASSED로 종료
    - test_message_bus.py에 `xfail` 마커가 없음
  </acceptance_criteria>
  <done>test_message_bus.py 5개 테스트 전부 PASSED, MessageBus publish/consume/ack 동작 확인</done>
</task>

</tasks>

<verification>
```bash
cd /Users/johyeonchang/ai-office/server && uv run pytest tests/test_message_bus.py -v --tb=short
```
5개 테스트가 모두 PASSED여야 한다.
</verification>

<success_criteria>
- test_publish_and_consume PASSED
- test_ack_removes_from_pending PASSED
- test_consume_filters_by_recipient PASSED
- test_consume_respects_limit PASSED
- test_atomic_write_pattern PASSED
- server/db/client.py에 PRAGMA journal_mode=WAL, PRAGMA busy_timeout=5000 모두 포함
</success_criteria>

<output>
완료 후 `.planning/phases/01-infra-foundation/01-02-SUMMARY.md` 생성
</output>
