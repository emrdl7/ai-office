---
phase: 01-infra-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/pyproject.toml
  - server/main.py
  - server/db/__init__.py
  - server/db/client.py
  - server/bus/__init__.py
  - server/bus/schemas.py
  - server/bus/message_bus.py
  - server/runners/__init__.py
  - server/runners/claude_runner.py
  - server/runners/ollama_runner.py
  - server/log_bus/__init__.py
  - server/log_bus/event_bus.py
  - server/workspace/__init__.py
  - server/workspace/manager.py
  - server/tests/__init__.py
  - server/tests/conftest.py
  - server/tests/test_message_bus.py
  - server/tests/test_claude_runner.py
  - server/tests/test_ollama_runner.py
  - server/tests/test_log_bus.py
  - server/tests/test_json_parser.py
  - server/tests/test_workspace.py
  - agents/.gitkeep
  - workspace/.gitkeep
  - data/.gitkeep
autonomous: true
requirements:
  - INFR-01
  - INFR-02
  - INFR-03
  - INFR-04
  - INFR-05
  - ARTF-01
  - ARTF-02

must_haves:
  truths:
    - 'uv run pytest -x -q 가 실행되어 테스트가 수집되고 xfail 또는 skip으로 종료된다 (에러 없음)'
    - '프로젝트 디렉토리 구조(server/, agents/, workspace/, data/)가 존재한다'
    - '모든 테스트 파일이 테스트 stub을 포함하며 pytest가 수집할 수 있다'
    - 'server/pyproject.toml에 fastapi, uvicorn, sqlmodel, httpx, pytest, pytest-asyncio가 선언된다'
  artifacts:
    - path: 'server/pyproject.toml'
      provides: 'uv 프로젝트 정의, 의존성 선언'
      contains: 'fastapi'
    - path: 'server/bus/schemas.py'
      provides: 'AgentMessage Pydantic 스키마 (D-02, D-03)'
      exports: ['AgentMessage', 'MessageType', 'AgentId']
    - path: 'server/tests/conftest.py'
      provides: 'pytest fixture (인메모리 SQLite, 임시 workspace 디렉토리)'
    - path: 'server/tests/test_message_bus.py'
      provides: 'INFR-01 test stub'
    - path: 'server/tests/test_workspace.py'
      provides: 'ARTF-01, ARTF-02 test stub'
  key_links:
    - from: 'server/tests/conftest.py'
      to: 'server/db/client.py'
      via: 'in-memory SQLite fixture'
      pattern: ':memory:'
    - from: 'server/bus/schemas.py'
      to: 'server/bus/message_bus.py'
      via: 'AgentMessage import'
      pattern: 'from.*schemas import AgentMessage'
---

<objective>
프로젝트 디렉토리 구조를 생성하고, uv Python 환경을 구성하고, 모든 인프라 컴포넌트의 빈 구현체(stub)와 실패하는 테스트 파일을 만든다. 이후 Wave 1~3 플랜들이 이 scaffold 위에 실제 구현을 채운다.

Purpose: 전체 Phase 1의 테스트 인프라와 파일 구조를 선행 확립하여, 이후 각 플랜이 독립적으로 특정 컴포넌트만 구현할 수 있게 한다. Nyquist 원칙: 모든 테스트가 Wave 0에서 먼저 존재해야 이후 태스크가 `<automated>` verify를 가질 수 있다.
Output: uv 환경 + 전체 파일 트리 + 실행 가능한(stub) 테스트 스위트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infra-foundation/01-CONTEXT.md
@.planning/phases/01-infra-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: uv 프로젝트 초기화 및 의존성 설치</name>
  <files>
    server/pyproject.toml
    agents/.gitkeep
    workspace/.gitkeep
    data/.gitkeep
  </files>
  <read_first>
    - .planning/phases/01-infra-foundation/01-RESEARCH.md (Standard Stack 섹션 — 라이브러리 버전 목록)
    - .planning/phases/01-infra-foundation/01-CONTEXT.md (D-04 디렉토리 구조 결정)
  </read_first>
  <action>
프로젝트 루트(/Users/johyeonchang/ai-office/)에서 다음 명령어를 순서대로 실행한다:

1. 디렉토리 생성:
```bash
mkdir -p agents workspace data
touch agents/.gitkeep workspace/.gitkeep data/.gitkeep
```

2. uv로 server 서브프로젝트 초기화 (Python 3.12):
```bash
cd /Users/johyeonchang/ai-office
uv init --python 3.12 server
```

3. server/ 디렉토리로 이동 후 의존성 추가:
```bash
cd server
uv add "fastapi>=0.135.0" "uvicorn[standard]>=0.34.0" "sqlmodel>=0.0.22" "pydantic-settings>=2.0.0" "httpx>=0.28.0"
uv add --dev "pytest>=9.0.0" "pytest-asyncio>=0.24.0"
```

4. server/pyproject.toml에 pytest 설정 추가 (uv init이 생성한 파일을 편집):
```toml
[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run python -c "import fastapi, sqlmodel, httpx, pydantic_settings; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - server/pyproject.toml 존재하며 `fastapi` 문자열 포함
    - server/pyproject.toml에 `pytest-asyncio` 문자열 포함
    - server/pyproject.toml에 `asyncio_mode = "auto"` 포함
    - agents/.gitkeep, workspace/.gitkeep, data/.gitkeep 파일 존재
    - `uv run python -c "import fastapi"` 가 0 exit code로 종료
  </acceptance_criteria>
  <done>uv 가상환경 구성 완료, 핵심 의존성 임포트 가능, 디렉토리 트리 존재</done>
</task>

<task type="auto">
  <name>Task 2: 컴포넌트 stub 파일 및 테스트 scaffold 생성</name>
  <files>
    server/main.py
    server/db/__init__.py
    server/db/client.py
    server/bus/__init__.py
    server/bus/schemas.py
    server/bus/message_bus.py
    server/runners/__init__.py
    server/runners/claude_runner.py
    server/runners/ollama_runner.py
    server/log_bus/__init__.py
    server/log_bus/event_bus.py
    server/workspace/__init__.py
    server/workspace/manager.py
    server/tests/__init__.py
    server/tests/conftest.py
    server/tests/test_message_bus.py
    server/tests/test_claude_runner.py
    server/tests/test_ollama_runner.py
    server/tests/test_log_bus.py
    server/tests/test_json_parser.py
    server/tests/test_workspace.py
  </files>
  <read_first>
    - .planning/phases/01-infra-foundation/01-RESEARCH.md (Pattern 1~5 코드 예시, 메시지 스키마 정의)
    - .planning/phases/01-infra-foundation/01-VALIDATION.md (Wave 0 Requirements 체크리스트)
    - server/pyproject.toml (생성된 파일 확인 후 작업)
  </read_first>
  <action>
아래 파일들을 정확한 내용으로 생성한다. 각 stub은 이후 플랜에서 실제 구현으로 교체된다.

**server/db/__init__.py** — 빈 파일

**server/db/client.py** — DB 연결 stub:
```python
# SQLite WAL 모드 연결 관리 (INFR-01)
# 실제 구현: 01-02-PLAN
import sqlite3
from pathlib import Path

DB_PATH = Path('data/bus.db')

def get_connection() -> sqlite3.Connection:
    raise NotImplementedError('01-02-PLAN에서 구현 예정')
```

**server/bus/__init__.py** — 빈 파일

**server/bus/schemas.py** — D-02, D-03에 따른 AgentMessage 스키마 (실제 구현, stub 아님):
```python
# 에이전트 간 메시지 스키마 (D-02: 풍부한 필드, D-03: 3가지 타입 + 확장 가능)
from pydantic import BaseModel, Field
from typing import Literal, Optional, Any
from datetime import datetime
import uuid

MessageType = Literal['task_request', 'task_result', 'status_update']
AgentId = Literal['claude', 'planner', 'developer', 'designer', 'qa', 'orchestrator']
Priority = Literal['normal', 'high', 'urgent']
Status = Literal['pending', 'processing', 'done', 'failed']

class AgentMessage(BaseModel):
    id: str = Field(default_factory=lambda: str(uuid.uuid4()))
    type: MessageType
    from_agent: AgentId = Field(alias='from')
    to_agent: AgentId | Literal['broadcast'] = Field(alias='to')
    payload: Any
    reply_to: Optional[str] = None
    priority: Priority = 'normal'
    tags: list[str] = Field(default_factory=list)
    metadata: dict[str, Any] = Field(default_factory=dict)
    created_at: datetime = Field(default_factory=datetime.utcnow)
    ack_at: Optional[datetime] = None
    status: Status = 'pending'

    model_config = {'populate_by_name': True}
```

**server/bus/message_bus.py** — 메시지 버스 stub:
```python
# SQLite WAL 기반 메시지 버스 (INFR-01)
# 실제 구현: 01-02-PLAN
from .schemas import AgentMessage

class MessageBus:
    def __init__(self, db_path: str = 'data/bus.db'):
        raise NotImplementedError('01-02-PLAN에서 구현 예정')

    def publish(self, message: AgentMessage) -> None:
        raise NotImplementedError

    def consume(self, to_agent: str, limit: int = 10) -> list[AgentMessage]:
        raise NotImplementedError

    def ack(self, message_id: str) -> None:
        raise NotImplementedError
```

**server/runners/__init__.py** — 빈 파일

**server/runners/claude_runner.py** — Claude runner stub:
```python
# Claude CLI subprocess 러너 (INFR-02, D-05, D-06)
# 실제 구현: 01-04-PLAN

async def run_claude_isolated(prompt: str) -> str:
    raise NotImplementedError('01-04-PLAN에서 구현 예정')
```

**server/runners/ollama_runner.py** — Ollama runner stub:
```python
# Ollama HTTP 클라이언트 + 단일 요청 큐 (INFR-03, INFR-05)
# 실제 구현: 01-05-PLAN

class OllamaRunner:
    def __init__(self, model: str = 'gemma4:26b'):
        raise NotImplementedError('01-05-PLAN에서 구현 예정')

    async def start(self):
        raise NotImplementedError

    async def stop(self):
        raise NotImplementedError

    async def generate(self, prompt: str) -> str:
        raise NotImplementedError
```

**server/log_bus/__init__.py** — 빈 파일

**server/log_bus/event_bus.py** — 이벤트 버스 stub:
```python
# asyncio.Queue 기반 인-프로세스 이벤트 버스 (INFR-04)
# 실제 구현: 01-06-PLAN
import asyncio
from dataclasses import dataclass, field
from typing import Any

@dataclass
class LogEvent:
    agent_id: str
    event_type: str
    message: str
    data: dict[str, Any] = field(default_factory=dict)

class EventBus:
    def subscribe(self) -> asyncio.Queue:
        raise NotImplementedError('01-06-PLAN에서 구현 예정')

    def unsubscribe(self, q: asyncio.Queue):
        raise NotImplementedError

    async def publish(self, event: LogEvent):
        raise NotImplementedError
```

**server/workspace/__init__.py** — 빈 파일

**server/workspace/manager.py** — workspace 관리자 stub:
```python
# 태스크별 격리 디렉토리 + atomic write (ARTF-01, ARTF-02)
# 실제 구현: 01-03-PLAN
from pathlib import Path

class WorkspaceManager:
    def __init__(self, task_id: str):
        raise NotImplementedError('01-03-PLAN에서 구현 예정')

    def safe_path(self, relative_path: str) -> Path:
        raise NotImplementedError

    def write_artifact(self, relative_path: str, content: str | bytes) -> Path:
        raise NotImplementedError
```

**server/main.py** — FastAPI 앱 stub:
```python
# FastAPI 오케스트레이션 서버 진입점
# WebSocket 엔드포인트는 01-06-PLAN에서 추가됨
from fastapi import FastAPI

app = FastAPI(title='AI Office')

@app.get('/health')
async def health():
    return {'status': 'ok'}
```

**server/tests/__init__.py** — 빈 파일

**server/tests/conftest.py** — 공유 fixture:
```python
# pytest 공유 fixture: 인메모리 SQLite, 임시 workspace 디렉토리
import pytest
import sqlite3
import tempfile
from pathlib import Path

@pytest.fixture
def in_memory_db():
    '''인메모리 SQLite 연결 — WAL 모드 테스트용'''
    conn = sqlite3.connect(':memory:')
    conn.execute('PRAGMA journal_mode=WAL')
    conn.execute('PRAGMA synchronous=NORMAL')
    conn.execute('PRAGMA foreign_keys=ON')
    conn.row_factory = sqlite3.Row
    yield conn
    conn.close()

@pytest.fixture
def tmp_workspace(tmp_path):
    '''임시 workspace 루트 디렉토리'''
    ws = tmp_path / 'workspace'
    ws.mkdir()
    return ws
```

**server/tests/test_message_bus.py** — INFR-01 stub:
```python
# INFR-01: SQLite WAL 메시지 버스 테스트
# 실제 테스트: 01-02-PLAN에서 구현
import pytest

@pytest.mark.xfail(reason='01-02-PLAN에서 구현 예정', strict=False)
def test_publish_and_consume(in_memory_db):
    '''메시지 발행 후 소비 왕복 테스트'''
    pass

@pytest.mark.xfail(reason='01-02-PLAN에서 구현 예정', strict=False)
def test_ack_removes_from_pending(in_memory_db):
    '''ACK 후 메시지가 pending에서 제거됨을 확인'''
    pass

@pytest.mark.xfail(reason='01-02-PLAN에서 구현 예정', strict=False)
def test_atomic_write_pattern(in_memory_db):
    '''tmp+rename atomic write 패턴 적용 확인'''
    pass
```

**server/tests/test_claude_runner.py** — INFR-02 stub:
```python
# INFR-02: Claude CLI subprocess 러너 테스트
# 실제 테스트: 01-04-PLAN에서 구현
import pytest

@pytest.mark.xfail(reason='01-04-PLAN에서 구현 예정', strict=False)
async def test_run_claude_returns_text():
    '''Claude subprocess가 텍스트 응답을 반환'''
    pass

@pytest.mark.xfail(reason='01-04-PLAN에서 구현 예정', strict=False)
async def test_bare_flag_token_isolation():
    '''--bare 플래그로 CLAUDE.md 주입이 차단됨'''
    pass
```

**server/tests/test_ollama_runner.py** — INFR-03 stub:
```python
# INFR-03: Ollama 러너 단일 큐 테스트
# 실제 테스트: 01-05-PLAN에서 구현
import pytest

@pytest.mark.xfail(reason='01-05-PLAN에서 구현 예정', strict=False)
async def test_sequential_queue_ordering():
    '''동시 요청이 순차적으로 처리됨을 확인'''
    pass
```

**server/tests/test_log_bus.py** — INFR-04 stub:
```python
# INFR-04: asyncio.Queue 이벤트 버스 테스트
# 실제 테스트: 01-06-PLAN에서 구현
import pytest

@pytest.mark.xfail(reason='01-06-PLAN에서 구현 예정', strict=False)
async def test_publish_reaches_subscriber():
    '''발행된 이벤트가 구독자 큐에 도달함'''
    pass

@pytest.mark.xfail(reason='01-06-PLAN에서 구현 예정', strict=False)
async def test_unsubscribe_stops_delivery():
    '''unsubscribe 후 이벤트가 전달되지 않음'''
    pass
```

**server/tests/test_json_parser.py** — INFR-05 stub:
```python
# INFR-05: Gemma4 JSON 파싱+복구 전략 테스트
# 실제 테스트: 01-05-PLAN에서 구현
import pytest

@pytest.mark.xfail(reason='01-05-PLAN에서 구현 예정', strict=False)
def test_strict_json_parse_valid():
    '''유효한 JSON 문자열이 파싱됨'''
    pass

@pytest.mark.xfail(reason='01-05-PLAN에서 구현 예정', strict=False)
def test_repair_json_with_trailing_text():
    '''후행 텍스트가 있는 JSON이 복구됨'''
    pass

@pytest.mark.xfail(reason='01-05-PLAN에서 구현 예정', strict=False)
def test_repair_returns_none_on_unrecoverable():
    '''복구 불가능한 출력에서 None 반환'''
    pass
```

**server/tests/test_workspace.py** — ARTF-01, ARTF-02 stub:
```python
# ARTF-01, ARTF-02: workspace 파일시스템 테스트
# 실제 테스트: 01-03-PLAN에서 구현
import pytest

@pytest.mark.xfail(reason='01-03-PLAN에서 구현 예정', strict=False)
def test_write_artifact_creates_file(tmp_workspace):
    '''write_artifact가 실제 파일을 생성함'''
    pass

@pytest.mark.xfail(reason='01-03-PLAN에서 구현 예정', strict=False)
def test_atomic_write_no_partial_file(tmp_workspace):
    '''쓰기 중단 시 부분 파일이 남지 않음'''
    pass

@pytest.mark.xfail(reason='01-03-PLAN에서 구현 예정', strict=False)
def test_path_traversal_blocked(tmp_workspace):
    '''경로 순회 공격(../etc/passwd)이 차단됨'''
    pass

@pytest.mark.xfail(reason='01-03-PLAN에서 구현 예정', strict=False)
def test_multiple_artifact_types_supported(tmp_workspace):
    '''.py, .md, .json 등 다양한 파일 형식 저장 가능'''
    pass
```
  </action>
  <verify>
    <automated>cd /Users/johyeonchang/ai-office/server && uv run pytest -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - server/bus/schemas.py에 `class AgentMessage` 포함
    - server/bus/schemas.py에 `MessageType = Literal['task_request', 'task_result', 'status_update']` 포함
    - server/tests/conftest.py에 `':memory:'` 문자열 포함
    - server/tests/conftest.py에 `def in_memory_db` 함수 존재
    - server/tests/conftest.py에 `def tmp_workspace` 함수 존재
    - `uv run pytest -x -q` 가 `error` 없이 종료 (xfail/skip은 정상)
    - pytest가 최소 7개 테스트 파일 수집 (test_message_bus, test_claude_runner, test_ollama_runner, test_log_bus, test_json_parser, test_workspace, test_claude_runner 포함)
    - server/main.py에 `from fastapi import FastAPI` 포함
  </acceptance_criteria>
  <done>전체 stub 파일 트리 생성 완료, pytest가 모든 테스트를 xfail/skip으로 수집하고 에러 없이 종료</done>
</task>

</tasks>

<verification>
```bash
# 전체 테스트 스위트 실행
cd /Users/johyeonchang/ai-office/server && uv run pytest --tb=short -q

# 디렉토리 구조 확인
ls /Users/johyeonchang/ai-office/server/bus/ /Users/johyeonchang/ai-office/server/runners/ /Users/johyeonchang/ai-office/server/log_bus/ /Users/johyeonchang/ai-office/server/workspace/ /Users/johyeonchang/ai-office/server/tests/

# 스키마 임포트 확인
cd /Users/johyeonchang/ai-office/server && uv run python -c "from bus.schemas import AgentMessage, MessageType; print('schemas OK')"
```
</verification>

<success_criteria>
- `uv run pytest -x -q` 가 에러 없이 실행되며 xfail 메시지 포함
- server/bus/schemas.py에 AgentMessage 클래스가 type, from_agent, to_agent, payload, reply_to, priority, tags, metadata, created_at, ack_at, status 필드를 모두 포함
- 7개 테스트 파일이 모두 존재하며 각각 최소 1개 이상의 stub 테스트 함수 포함
- 프로젝트 루트에 agents/, workspace/, data/ 디렉토리 존재
</success_criteria>

<output>
완료 후 `.planning/phases/01-infra-foundation/01-01-SUMMARY.md` 생성
</output>
